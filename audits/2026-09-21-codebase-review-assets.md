# Audit 2026-09-21 — published assets review: `generator/`, `hooks-src/`, `scripts/`, `skills-src/`, `contracts/`, `pyproject.toml` (read-only)

**Method:** one read-only reviewer sub-agent (pi-archimedes dispatch, config-less). Ground truth: DN-multi-harness-deploy-model §D13/§D14/§D16, CR-MDB-025 (re-spec), CR-MDB-027; Pi measured locally at 0.86.1 (`@mariozechner/pi-coding-agent` docs + `dist/*.d.ts`). Nothing was modified. `python3 generator/build.py --check` → clean (agents mirror templates exactly, so every template defect is ×16 in `generator/agents/`). Companion audits: `-modelb_axi.md`, `-tests.md`, `-docs.md`. Consumed by CR-MDB-030/031 and the 2026-09-21 spec amendments.

Format: `file:line` | category | severity | evidence | action | **owner**.

## 1. `modelb_axi/hooks.py` `_emit_pi` + `hooks-src/` — the Pi hook path is dead end-to-end

1. `modelb_axi/hooks.py:281-284` | inconsistency | **high** | emits `pi.on("tool_call", async (payload) => {…})` at module top level — no `export default function (pi: ExtensionAPI)`; Pi 0.86.1 `extensions.md:63` requires the default-export factory | every emitted `.pi/extensions/<hook>.ts` throws `ReferenceError: pi` at load; wrap in the factory | **UNOWNED** — CR-MDB-019 Non-goals: "No change to the pi … emitters"; CR-MDB-025 §S5: "no hook emitter".
2. `modelb_axi/hooks.py:213-214` | inconsistency | **high** | `pi.exec(script, [], { stdin: JSON.stringify(payload) })` — `dist/core/exec.d.ts` `ExecOptions = {signal, timeout, cwd}`; no `stdin` | payload never reaches the script → `json.load(sys.stdin)` fails → every script `return 0` (fail-OPEN even for `fail_direction: closed`) | **UNOWNED** (same exclusions as #1).
3. `hooks-src/scripts/{block-write-outside-worktree:64,74-75; block-direct-cargo-test:118-120; block-direct-mvn-test:118-121; block-cr-completed-without-spec-update:51-53; post-regression-disk-reminder:41-43; block-bad-cycle-task-name:35-37}` | inconsistency | **high** | 6/7 scripts gate on `payload.get("tool_name")` ∈ {`Bash`,`Write`,`Edit`,`NotebookEdit`,`TaskCreate`} and read `tool_input.command/file_path` (35 occurrences). Pi `ToolCallEvent` carries `toolName` ∈ {`bash`,`read`,`write`,`edit`,`grep`,`find`,`ls`} and `input` (`types.d.ts:586-596`) | even with #2 fixed, every guard silently allows on Pi; port field names + lowercase tool names (write/edit path field is `path`, not `file_path`) | **UNOWNED**.
4. `hooks.py:259-266` `_PI_EVENTS` | inconsistency | med | "`pre-compact` has NO documented pi counterpart — DECLARED GAP" — Pi has `session_before_compact` (`extensions.md:408-413`, `types.d.ts:788`) | map it; drop the false degrade note | **UNOWNED**.
5. `hooks-src/schema.md:23` vs `hooks.py:190-200,281-296` | inconsistency | med | schema: "Harness compilers translate [matcher] to the native matcher syntax" — only `_emit_claude_code` consumes `matcher`; `_emit_pi` ignores it (fires on every `tool_call`, filtering delegated to the Claude-named gates in #3); scaffold matchers are `TaskCreate`/`Bash`/`Write|Edit` (`scaffold.py:351,358,366`) | translate matcher into a `toolName` predicate in the emitted TS, and re-vocabulary matchers to Pi names | **UNOWNED**.
6. `hooks-src/scripts/block-bad-cycle-task-name:35` + `scaffold.py:351` | dead | med | `payload.get("tool_name") != "TaskCreate"` — no `TaskCreate` tool exists on Pi (the local task tool is `todo`) | retarget or retire the hook | **UNOWNED**.
7. `hooks-src/scripts/block-write-outside-worktree:25` `_WORKTREES_SEGMENT = "/.claude/worktrees/"` · `scripts/worktree-flow.py:87` `WORKTREE_SUBDIR = ".claude/worktrees"` · `skills-src/{bootstrap:58, shutdown:49, model-b/references/orchestration-track:20, sub-agent-procedure:6}` | unwanted | med | 6 consumers agree on a Claude-Code-named worktree layout; `git worktree add … .claude/worktrees/<cr>` | one coordinated CR moving the convention (e.g. `.worktrees/`) across all 6 — it is a cross-consumer wire contract | **UNOWNED**.
8. `hooks.py:190-200 _emit_claude_code`, `:224-256 _emit_opencode`, `:316-338 _emit_hermes_advisory`, `_CLAUDE_EVENT_KEYS:183`, `_REFUSAL_REASONS:139`; `deploy.py:32-34 HARNESS_SKILL_DIRS = {"claude-code": ".claude/skills"}` | unwanted | med | three emitters + a live `.claude/skills` symlink target for a dropped harness; CR-MDB-019 §S3/§S4 spends effort improving `_emit_opencode` | retire non-Pi emitters/roster entries; re-scope 019 §S3 | **UNOWNED** — CR-025 §S5 explicitly freezes `HARNESS_ROSTER`/`HARNESS_SKILL_DIRS`/`hooks.py` while its own §S5 grep gate forbids `~/.claude` paths in `modelb_axi/` (contradiction inside 025).
9. `hooks-src/schema.md:7-8,54-56`; `block-direct-cargo-test:29-31,43-44` | inconsistency | med | "compiled per harness (claude-code, opencode, hermes, pi)"; "the crucible repo `clients/` dir in dev" — instructs the dev checkout as a client location | rewrite to the `~/.crucible/clients/` manifest contract only | CR-MDB-020 §S0 (docs AC covers "no Model B doc … instructs reading Crucible's own repository"); hook-script text not in 020's file list → **partially UNOWNED**.
10. `hooks-src/scripts/ambient-board-status:40` (`STATUS_CONTRACT_VERSION = "1.0.0"`, 5 refs) | inconsistency | med | document is 2.0.0 | re-pin | **CR-MDB-019 §S1**.
11. `ambient-board-status:45-51` | dead | med | `_STACK_MARKERS` has no arduino marker | add | **CR-MDB-019 §S2**.
12. `ambient-board-status:66` | dead | med | reads `[install].clients_dir` which `config.py` never writes → feed never resolves | **CR-MDB-018**.
13. `ambient-board-status:169` | inconsistency | low | `Next: plan-file --cr <CR-ID> --wave <n>` — `plan-file` now requires `--title` + `--cycle`/`--cycle-kind` | fix hint | CR-MDB-017 §S4a lists skills/contracts/AGENTS.md/docs only → **UNOWNED** for hooks-src.
14. `block-bad-cycle-task-name:44`, `block-direct-cargo-test:29,43` | inconsistency | low | cite "ORCHESTRATOR-RULES.md §2/§4" — a retired NAI memory doc | cite the `model-b` skill | **UNOWNED**.
15. `hooks-src/scripts/*:6-8` (6 files) | risk | low | "Normalized from the deployed `~/.claude/hooks/*.sh`" — provenance only, but 6 more `~/.claude` strings a grep gate will hit | reword as history | **UNOWNED**.

## 2. `generator/` — 16/16 definitions are Claude-Code-shaped and non-executable on Pi

16. `generator/stacks/{arduino,bun,python,quarkus}.toml:5-7` (12) → `generator/agents/*.md` (56) | inconsistency | **high** | `test_command = "python3 ~/.claude/scripts/python-crucible.py …"` — mirror retired by CR-016; must be `~/.crucible/clients/` | **CR-MDB-020 §S2/§S3**.
17. `generator/templates/{red,green,verify,fix}.md.tmpl:24/29/26/23` (4 → 16 agents) | inconsistency | **high** | `${register_command} --phase RED` — 400/409 against released Crucible | **CR-MDB-017 §S6**.
18. `generator/stacks/*.toml [frontmatter]` (16 blocks; `bun.toml:35-44`, `python.toml:41-53` incl. `tools: Read, Grep, Glob, Bash`) | inconsistency | **high** | `model: sonnet|inherit` (archimedes passes it literally as `--model` → resolves to nothing), `effort`/`color`/`maxTurns`/`skills:` inert, capitalised tool names grant **none** of Pi's tools | **CR-MDB-025 §S1–§S3** (fully specced).
19. `generator/templates/*.tmpl:10-11` (8 → 32 in agents) | inconsistency | **high** | `~/.claude/skills/model-b/references/sub-agent-procedure.md`, `~/.claude/skills/crucible/SKILL.md` — store is `~/.agents/skills/` | repoint | CR-MDB-025 §S0 parks "020 or 025?"; CR-020 §S2 says `crucible_reference` "continues to point at … the deployed skill store" but never repoints it → **contested / effectively UNOWNED**.
20. `generator/stacks/*.toml:8` `crucible_reference = "~/.claude/skills/crucible/references/<stack>.md"` (4 → 16) | inconsistency | high | same as #19 | same | **contested (020 §S2 vs 025 §S0)**.
21. `generator/templates/*.tmpl` — 30 tool-name instructions (`red.md.tmpl:26,42-44,52`: "Run via `Bash`", "**Read** … **Glob** … **Bash**", "New test file: `Write`… `Edit`"; `verify.md.tmpl:17,35`: "any Write/Edit/NotebookEdit") + `CLAUDE.md` as project context (`red:27, green:32, verify:29, fix:26`) | inconsistency | med | Claude Code tool vocabulary and Claude-only context file; Pi reads AGENTS.md and lowercase tools | rewrite body vocabulary once, in the same regeneration as 017/025 | **UNOWNED** — CR-025 §S2 translates *frontmatter* tools only; Non-goals: "No change to the sub-agent procedure".
22. `generator/stacks/quarkus.toml:17` (→ 4 agents, 10 refs) | inconsistency | med | `~/.claude/memory/java-testing-practices.md`, `~/.claude/memory/java-coding-standards.md` — live `~/.claude` memory paths | repoint or drop | **UNOWNED** (CR-006's "language refs stay global" predates §D14).
23. `generator/stacks/arduino.toml:4-8` | inconsistency/risk | med | `display_name = "Arduino firmware (Sheetal)"`, `--project-dir sheetal-firmware` hard-coded in all three commands, "no per-stack reference file for arduino yet" (but `skills-src/crucible/references/arduino.md` exists), "`regression` tier … lands with CR-SHE-006 — until then use `unit`" (crucible/SKILL.md:46 already lists `regression`) | de-project the stack; fix the reference claim | CR-MDB-017 §S6b (tier-honourability) partial; Sheetal residue **UNOWNED**.
24. `generator/stacks/bun.toml:10-18,49-55,62-73` | risk | med | stack mechanics are one project's (Sandesh Pi-extension shim: `integrations/pi/`, "SUT is a thin TS shim that shells to the `sandesh` CLI", "PE3", `pi.exec` mocks) presented as generic Bun/TS guidance | generalise; move project specifics to a memory template | **UNOWNED**.
25. `generator/stacks/{python,bun,quarkus,arduino}.toml [frontmatter] skills:` (`reviewer`, `reviewer-*`, `refactorer-java` — not Model B bundles) | dead | low | archimedes has no autoload; the names are not published by Model B | settle per CR-025 §S0(3) | **CR-MDB-025 §S0**.
26. `generator/build.py:21-23` | risk | low | "The 13 bespoke defs (rust x4, vscode x4, electronics x4, inbox-analyst) are NEVER targets" — rust becomes a target (024); electronics named | update docstring | **CR-MDB-024** (rust); electronics wording **UNOWNED**.

## 3. `scripts/` and `contracts/` vs their implementations

27. `scripts/worktree-flow.py:100,1015,1060,1100,1130,1163` | unwanted | med | `import schedule_db as _sdb` + 5 DB-coupled verbs | **CR-MDB-028 §S1/§S2** (schedule_db.py itself stays shipped, TRANSITIONAL — 028 §S5).
28. `scripts/skill-release-gate.py:129-131` | inconsistency | low | auto-discovers `"skills"` and `".claude/skills"` project dirs; Pi's are `.pi/skills/` / `.agents/skills/` | add Pi roots, drop `.claude` | **UNOWNED**.
29. `contracts/crucible-envelope.md:129-131,136` | inconsistency | high | `plan-file … --cycles <n> [--orchestrator <id>]`, `gate-report` as the gate verb — all retired/refused on 0.2.2 | **CR-MDB-017 §S4a/§S4b**.
30. `contracts/crucible-envelope.md:11-13,15,42-46` + `tests/test_toon_codec.py:99` | inconsistency | high | "codec is `crucible:clients/toon.py`… conformance PROVEN by round-tripping through `crucible:clients/toon.py` out of process" — `crucible:` = `~/Documents/data_projects/crucible` (dev checkout, out of bounds); the contract *mandates* the forbidden oracle | re-anchor the oracle on `~/.crucible/clients/toon.py` (installed) or drop the cross-project proof | CR-MDB-020 §S0 (doc text) partial; oracle design **UNOWNED**.
31. `contracts/crucible-envelope.md:4-5,18-19` | inconsistency | med | "TRACKS CR-CRU-030 + CR-CRU-036 (crucible develop `949a2f4`)" — pinned to a develop commit, not release 0.2.2; "GETs additionally serve compact TOON via `?fmt=toon`" — TOON removed server-side (CR-CRU-132) | re-pin to 0.2.2, delete the GET claim | **UNOWNED** (017 re-records role/cycle facts only).
32. `contracts/crucible-envelope.md:114`; `skills-src/crucible/SKILL.md:91`; `crucible/references/*.md:15-17` (6); `chezmoi/SKILL.md:52-53,86-88`; `contracts/lean-ctx.md:36` — **14 occurrences** | unwanted | med | `/tmp/claude-1000/<project>-crucible` — Claude Code's per-uid tmp dir as the sanctioned context wrapper | name a harness-neutral wrapper location or retire the wrapper idiom | **UNOWNED** (README 2026-07-21 said it "dies at the 013 re-plan wrapper rewrite"; it did not).
33. `contracts/gate-lock.md:4,29-32,37-40,66` vs `scripts/gate-lock.sh` | inconsistency | med | contract cites `crucible:clients/rust-crucible.py::_gate_lock_path()` and says `tests/test_tooling_detachment.py` evaluates it "out of process" — `tests/test_tooling_detachment.py:375` hard-codes `~/Documents/data_projects/crucible/clients/rust-crucible.py` | re-anchor on `~/.crucible/clients/rust-crucible.py` | **CR-MDB-020 §S0** (partial — tests not in its scope).
34. `contracts/gate-lock.md:52-56` vs `scripts/gate-lock.sh:122` | inconsistency | low | `check` exits `1` on resources-loaded; contract's exit table defines `1 = held by another track`, `2 = resources loaded` (script header line 33 says "check … (0/1)") | document `check`'s own 0/1 semantics | **UNOWNED**.
35. `scripts/gate-lock.sh:3-4` | inconsistency | low | "Consuming skills: none in-tree yet — CR-MDB-022 §S5 names it" — 022 shipped; `orchestration-common.md:63` names it | fix header | **UNOWNED**.
36. `contracts/lean-ctx.md:15-16,24-26,33,36` | unwanted | med | `~/.claude/rules/lean-ctx.md` "imported by the global CLAUDE.md"; preference order in Claude tool names (`Read/Grep/Edit/StrReplace/Glob`); "every `~/.claude` mutation … goes through chezmoi"; `/tmp/claude-1000` | rewrite against Pi + the pi-lean-ctx extension | **UNOWNED**.
37. `contracts/mail-axi.md` (whole file; `:10,14-15,46`) | dead | med | "Owner: TBD — a NEW tool"; sources `~/.claude/skills/mail-tracking-core/SKILL.md`, `mcp__claude_ai_Gmail__*` — a personal mail-assistant requirements doc, not a Model B interface; superseded by the external `vidushi-oa`/`voa` CLI; ships in the wheel | remove from `contracts/` (archive) | **UNOWNED**.
38. `pyproject.toml:26` `"contracts" = "modelb_axi/_assets/contracts"` | dead | low | `deploy.py` deploys `skills-src`, `hooks-src/scripts`, `scripts` only; nothing reads `contracts/` at runtime — wheel dead weight (carrying #37) | drop from force-include or add a consumer | **UNOWNED**. (Other force-includes match real asset dirs; `generator` is consumed by `scaffold.py:304`.)
39. `pyproject.toml:8` `version = "0.1.0.dev0"` | inconsistency | low | target release renamed 1.0.0 | **CR-MDB-012**.

## 4. `skills-src/` — technical claims and paths

40. `skills-src/crucible/SKILL.md:51`; `crucible/references/{arduino:12,bun:12,java:11,python:12,rust:11}.md` — **6** | inconsistency | **high** | `~/Documents/data_projects/crucible/clients/<stack>-crucible.py` documented as "the source of truth" — binds runs to the dev board (measured near-miss, README 2026-09-18) | **CR-MDB-020 §S0**.
41. `crucible-report-{arduino:37,bun:36,java:32,python:39,rust:38}/SKILL.md` + others — **36** unrooted `clients/<stack>-crucible.py` | inconsistency | high | **CR-MDB-020 §S1**.
42. `--phase` — `crucible/SKILL.md:17,35`; 5 report bundles (`:37/36/32/39/38`); `memory-templates/java-orchestration.md:17` — **9** (27 with generator) | inconsistency | high | **CR-MDB-017**.
43. `crucible/SKILL.md:95-97`; `model-b/SKILL.md:47` | inconsistency | high | `plan-file --cycles <n> … --orchestrator <id>`, `gate-report` | **CR-MDB-017 §S4a/§S4b**.
44. `~/.claude/skills/...` in skill bodies — `bootstrap:85,86,87,93,98`; `shutdown:70,75`; `crucible:122`; `git-workflow:61`; `memory-templates/java-orchestration:5`, `rust-orchestration:5,19` — **12** | inconsistency | **high** | e.g. bootstrap:85 "**`~/.claude/skills/model-b/references/orchestration-common.md`** — universal orchestrator rules" — the store is `~/.agents/skills/` | repoint to `~/.agents/skills/<name>/…` (or relative skill-dir paths) | **UNOWNED** (CR-025 §S0 asks only about template bodies).
45. `skills-src/bootstrap/SKILL.md:89,92` | unwanted | med | `~/.claude/projects/-home-antonyj-Documents-data-projects-nai/memory/{ORCHESTRATOR-NAI,MEMORY}.md` — machine-specific Claude Code project memory | remove NAI examples | **UNOWNED**.
46. `memory-templates/java-orchestration.md:16`, `rust-orchestration.md:10` | inconsistency | high | `~/.claude/scripts/{mvn,rust}-crucible.py` | **CR-MDB-020 §S2**.
47. Claude Code tools/params in orchestration skills: `EnterWorktree` (`orchestration-common:51`, `orchestration-track:19`, +1), `ExitWorktree` ×4, `TaskList`/`TaskUpdate` (`bootstrap:137,139`), `TaskStop` (`shutdown:187`), `reference-task-panel-resume-bug` (`bootstrap:140`), `run_in_background` ×6 (`bootstrap:119,211`; `sandesh.md:16`; `orchestration-track:39`; `crucible:114`; `shutdown:187`), `PreToolUse`/`Write`/`Edit`/`NotebookEdit`/`dangerouslyDisableSandbox` (`sub-agent-procedure:10`) | inconsistency | **high** | e.g. `orchestration-track.md:39` "Dispatch RED/GREEN/VERIFY/FIX `run_in_background`" — archimedes dispatch is blocking with per-call `cwd`; `EnterWorktree` has no Pi equivalent | rewrite the dispatch/worktree/todo mechanics for Pi + archimedes | **CR-MDB-026 §S1–§S3** owns only the watcher-launch `run_in_background` (bootstrap/shutdown/sandesh); dispatch-in-background, `EnterWorktree`, `Task*`, `PreToolUse` wording **UNOWNED**.
48. `skills-src/chezmoi/SKILL.md` (whole bundle: 92 lines, 35 `chezmoi` refs, all about `~/.claude`); `git-workflow/SKILL.md:151`; `contracts/lean-ctx.md:33`; `AGENTS.md` lists `chezmoi` as an owned bundle and instructs chezmoi discipline | unwanted | **high** | description: "Chezmoi discipline for the ~/.claude dotfiles tree … any ~/.claude edit" — Model B "carries no chezmoi dependency" (directive 2026-08-27) and `~/.claude` is not a live path | retire the bundle (installer manifest + `tests/test_installer_assets.py` name list + AGENTS.md) | **UNOWNED** — CR-MDB-021 retires chezmoi from *tests* only.
49. `memory-templates/rust-orchestration.md:12-23,33`; `java-orchestration.md:28`; `orchestration-track.md:12` | unwanted | med | document `cs`/`next`/`reconcile`/`.nai-schedule.db` as the lane plan | **CR-MDB-028 §S3** (lists bootstrap + model-b refs + AGENTS.md; the two memory templates are missing from its §S3/§S7 list → **partial**).
50. `crucible/SKILL.md:47` | inconsistency | med | "vscode | NO client yet … the future client is CRUCIBLE's deliverable (thread #1322)" — declined 2026-09-16 (#1370): must NOT be built | rewrite as "editor overlay on bun; direct v2 path by design" | **UNOWNED**.
51. `crucible/SKILL.md:108-110` | inconsistency | low | "GETs also serve compact TOON (`?fmt=toon` / `Accept: text/toon`)" — removed server-side (CR-CRU-132) | delete | **UNOWNED**.
52. `crucible/SKILL.md:48`, `model-b/SKILL.md:62` | — | ok | electronics appears only as an explicit *exclusion* row/sentence — not residue; no action.
53. `orchestration-common.md:9`, `orchestration-mainline.md:3`, `orchestration-track.md:3` | inconsistency | low | "sub-agent procedure → AGENTS.md" — it lives at `references/sub-agent-procedure.md` (whose own H1 is still `# AGENTS`) | fix pointers/H1 | **UNOWNED**.
54. `orchestration-common.md:72`; `memory-templates/java-orchestration.md:47` | dead | low | "Run the stack's `check-cr-close` gate" — no such tool in `scripts/` or the wheel | name a real verb (`cr-close`) or drop | **UNOWNED**.
55. `sub-agent-procedure.md:15` vs `crucible/SKILL.md:17-22` | inconsistency | low | "Heartbeat ~every 2 min" vs "ingest remains the heartbeat; touch only on a genuine status change" | align on the crucible skill | **UNOWNED**.
56. `crucible-report-python/SKILL.md:82`, `crucible-report-rust/SKILL.md:114` | inconsistency | low | raw-curl example sets `"source": "claude-code"` in the identity payload | drop/neutralise when 017 rewrites these bundles | **CR-MDB-017** (same files) / else UNOWNED.
57. `skills-src/CRUCIBLE-HANDOVER.md:9` | risk | low | "Origin repo: `~/Documents/data_projects/crucible`" — provenance-only, but CR-020 §S0 AC demands zero such paths outside `archive/` | move the record to `archive/` or exempt it in the gate | **CR-MDB-020 §S0** (conflict to resolve at gap-analysis).
58. `bootstrap/SKILL.md:36`, `shutdown/SKILL.md:52`, `java-orchestration.md:71` (+ templates, #21) | inconsistency | low | "derive `<Project>` from the repo's CLAUDE.md" — Pi projects carry AGENTS.md | say AGENTS.md | **UNOWNED**.

## 5. Out-of-scope but load-bearing (noted, not counted)

59. `tests/{test_hooks:415, test_installer_assets:80, test_scaffold:52, test_skills_handover:49, test_tooling_detachment:375, test_toon_codec:99}` | risk | high | six test modules hard-code `~/Documents/data_projects/crucible/clients` as oracle/origin — the checkout the ground truth places out of bounds; the contracts in #30/#33 direct them there | **UNOWNED** (CR-020 scopes `skills-src/`+`generator/`+`docs`; tests excluded).
60. `docs/research/DN-multi-harness-deploy-model.md:582` (012 row) still says "Claude Code output is byte-identical" although §D14 retired that constraint | **CR-MDB-012** spec authoring should not inherit it.

## Summary (5 lines)

1. **`~/.claude` is still the live path in 171 places inside the published surfaces** (templates 8, stacks 18, agents 112, hooks-src 6, skills-src 24, contracts 3); CR-020 owns the 70 client-mirror strings, CR-025 owns frontmatter, but the 76 `~/.claude/skills/...` body citations, `~/.claude/memory`, and NAI project-memory paths have no owner.
2. **The Pi hook pipeline cannot work at all on Pi 0.86.1**: no default-export factory, an `stdin` option `pi.exec` does not have, and Claude Code payload/tool names in 6/7 scripts — while CR-019 and CR-025 both explicitly exclude the Pi emitter from scope.
3. **Every generated agent definition is non-executable on Pi** (`--phase`, retired mirror path, Claude frontmatter, capitalised tools, `CLAUDE.md`) — 017/020/025 cover most, but body tool vocabulary, `crucible_reference`, Sheetal/Sandesh project residue in `arduino.toml`/`bun.toml` are contested or unowned.
4. **Dev-checkout coupling is written into the contract layer** (`crucible-envelope.md` oracle, `gate-lock.md`, 6 skill/reference files, hook block-reason text, 6 test modules) — CR-020 §S0 covers only the doc strings.
5. **Claude-era residue no CR retires**: the `chezmoi` bundle, `/tmp/claude-1000` wrapper (14), `contracts/mail-axi.md` + `lean-ctx.md`, `.claude/worktrees` convention (6 consumers), `EnterWorktree`/`Task*`/dispatch-`run_in_background`, and three non-Pi hook emitters that CR-025 §S5 freezes and CR-019 §S3 improves.

## Top 3

1. **Fix or re-own `_emit_pi` + the hook scripts' payload contract** (#1–#6): file a CR (or amend 019/025) — default-export factory, drop `stdin` (pass payload via argv/env or `exec` alternative), `toolName`/`input`/lowercase names, matcher→`toolName` predicate, `session_before_compact`. Until then every "hard-enforced" boundary the skills promise (`sub-agent-procedure.md:10`) is fail-open on the only target.
2. **Settle ownership of the `~/.claude/skills` body citations and `crucible_reference`** (#19, #20, #44) before 017/020/025 regenerate the fleet — otherwise one regeneration fixes the commands and leaves 76 dead skill paths in the same files.
3. **Retire the Claude-era substrate in one sweep** (#7, #32, #36, #37, #47, #48): `chezmoi` bundle, `/tmp/claude-1000` wrapper, `.claude/worktrees`, `EnterWorktree`/`Task*` mechanics, `contracts/mail-axi.md`/`lean-ctx.md`, and the non-Pi emitters — none is on any CR's scope list, and each re-teaches Claude Code to a Pi orchestrator.
