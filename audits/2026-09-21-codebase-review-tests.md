# Audit 2026-09-21 — `tests/` review (read-only)

**Method:** one read-only reviewer sub-agent (pi-archimedes dispatch, config-less) over all 19 test modules under `tests/` (AGENTS.md says 17; `test_tooling_adoption.py`, `test_tooling_detachment.py`, `test_toon_codec.py` were added by CR-MDB-022 and the count was never updated). One read-only run: `python3 -m unittest discover -s tests -t .` → **240 tests, 7 failures, 12 skips** (AGENTS.md baseline claims 240/1F/12S — stale). Ground truth: Pi-only target (DN §D13/§D14/§D16); chezmoi retired (CR-021); no `~/.claude` writes; store = `~/.agents/`; Crucible = production `127.0.0.1:3849` + `~/.crucible/clients/`; dev checkout `~/Documents/data_projects/crucible` out of bounds (2026-09-18 rule). Companion audits: `-modelb_axi.md`, `-assets.md`, `-docs.md`. Consumed by CR-MDB-032 and the 2026-09-21 spec amendments.

Severity: **H** = false-green/forbidden-dependency/blocks a filed CR · **M** = pins a retired contract or dropped target, will break on the next CR · **L** = hygiene.

## 1. Technical inconsistencies

### 1a. Forbidden reach into the Crucible DEV checkout (`~/Documents/data_projects/crucible/`) — 7 sites, 4 modules

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `tests/test_scaffold.py:51-53,131-139` (used at `:262,:285,:654`) | dev-checkout import | **H** | `TOON_CODEC_PATH = ~/Documents/data_projects/crucible/clients/toon.py`, loaded via `spec_from_file_location` and used as the decoder for 3 envelope tests. No skip guard — these 3 tests **hard-fail on any machine without the dev checkout**. Model B has its own `modelb_axi/toon.py` (CR-022) and `test_worktree_flow_axi.py:57` already shows the in-repo import pattern. | Replace with `from modelb_axi import toon`. **UNOWNED** (CR-022 §S2/AC6 gate missed it because `spec_from_file_location` is not an `import` node — see 1a-gap below). |
| `tests/test_hooks.py:415-429` | dev-checkout import, hard-coded absolute path | **H** | `CRUCIBLE_CLIENTS_DIR = Path("/home/antonyj/Documents/data_projects/crucible/clients")` — literal home path, not even `Path.home()`; `_toon_encode` loads their `toon.py` in-process to build fixtures for 3 `AmbientBoardStatusScriptTest` cases. | Use `modelb_axi.toon.encode`. **UNOWNED** (CR-019 touches this class for the STATUS-CONTRACT re-pin; fold in). |
| `tests/test_toon_codec.py:99-100,166-175` (`_require_oracle` at `:770`, used `:916,:997`) | dev-checkout subprocess oracle | **M** | Out-of-process oracle against `crucible:clients/toon.py`; skips when absent, so it is *silently green* on any other machine. The sanctioned installed copy `~/.crucible/clients/toon.py` exists on this machine (verified) and is byte-equivalent in role. | Point oracle at `~/.crucible/clients/toon.py` (resolved via `crucible-clients.json` per CR-018) and fail-not-skip when absent. **CR-MDB-020** (client-path anchoring). |
| `tests/test_tooling_detachment.py:374-376,565,698` | dev-checkout source read | **M** | Reads `rust-crucible.py` source text out of the dev checkout to extract `_gate_lock_path` for the S5 lock-path parity test; skips when absent. | Repoint to `~/.crucible/clients/rust-crucible.py`. **CR-MDB-020**. |
| `tests/test_skills_handover.py:48-50,189-193` · `tests/test_installer_assets.py:79-81,222-226` | dev-checkout dir probe + permanent skip | **M** | Both byte-identity / superset-fidelity tests check `~/Documents/data_projects/crucible/clients/skills` and `skipTest` when absent. The origin has retired (2 of today's 12 skips). These are **dead tests** — they can never run again by design. | Delete both; provenance is already pinned by `HandoverProvenanceDocTest`. **UNOWNED** (CR-016 close-out residue). |
| `tests/test_realhome_supersede.py:357,366` | remedy text names forbidden path | **L** | Failure message instructs: "must be repointed to the `~/Documents/data_projects/crucible/clients/` paths". | Reword to `~/.crucible/clients/`. **CR-MDB-020**. |
| `tests/test_toon_codec.py:346-368` (`_crucible_imports`) + `:314-343` (`_path_inserts_outside_repo`) | gate gap | **M** | The §S2/AC6 "no repo file imports a module from Crucible's checkout" gate only matches `import`/`from` nodes and literal `sys.path` args. It cannot see `importlib.util.spec_from_file_location(..., <checkout path>)` (`test_scaffold.py:134`, `test_hooks.py:424`) nor a checkout path embedded in a subprocess program string (`test_toon_codec.py:169` itself). The gate passes while 2 modules violate its intent. | Extend to `spec_from_file_location` calls whose path constant resolves outside the repo. **UNOWNED**. |

### 1b. Assertions against REAL home state (`~/.claude`, `~/.agents`) instead of a sandbox — 8 modules ungated

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `tests/test_core_split.py:18,48-49,113-114,177-179,222-223` · `test_model_b_skill.py:24-27` · `test_cr_authoring_skill.py:26-45` · `test_crucible_skill.py:23,64-70,296,371-375` · `test_git_chezmoi_skills.py:26-44` · `test_memory_model.py:28-32,48-51` | real-home structural gates (wave-1/2 CRs 001–006) | **H** (pattern ×6 modules, ~30 methods) | Every test asserts `Path.home()/.claude/{AGENTS.md,CLAUDE.md,skills,memory,agents}` content/existence/grep results — user dotfile state, not a Model B property. Contradicts the 2026-07-22 repo-local authoring rule ("AC gates assert repo paths"). They pass only because this machine's `~/.claude` happens to be in the wave-2 end-state. | Retarget to `skills-src/`, `archive/`, and the repo `AGENTS.md`; drop the memory-tree set-equality (`test_memory_model.py:265-283` asserts `~/.claude/memory` == exactly 6 files). Partially **CR-MDB-021** (chezmoi subset only); the rest **UNOWNED** — needs a "wave-1/2 gate relocation" CR. |
| `tests/test_worktree_flow_axi.py:35-37,75-110,113-188,190-233` | drives STALE deployed copies | **H** | `WORKTREE_FLOW = ~/.claude/scripts/worktree-flow.py`, `DEPLOYED_TOON = ~/.claude/scripts/toon.py`, `SKILLS_DIR = ~/.claude/skills`. `~/.agents/scripts/` does not exist on this machine (stale install), so the module exercises the chezmoi-managed copies CR-022 superseded — its 6 greens never touch `scripts/worktree-flow.py`. Line 78-110 pins the retired banner `DEPLOYED COPY`/`TRACKS` of the old file, and `:99` inserts `~/.claude/scripts` on `sys.path` inside a subprocess string (the AST gate cannot see it). README 2026-08-27 deferred items (i)/(ii) name exactly this and say "one CR's worth of work"; still unfiled. `test_toon_codec.py:601-626,1036-1081` already covers the same verbs against the in-repo script. | Delete `WorktreeFlowCodecDeploymentTest` and `WorktreeFlowEnvelopeTest` (superseded); retarget `WorktreeFlowSkillConsumerNotesTest` to `skills-src/*/SKILL.md`. **UNOWNED** (deferred register, 2026-08-27). |
| `tests/test_agent_generator.py:83,603-625` | real-home Claude Code agents dir | **M** | `DeployedAgentsConsumerConstraintTest` asserts all 16 generated files exist in `~/.claude/agents/`. Under §D14 that tree is "unowned legacy: never written, never deleted"; the installer will never redeploy there, so the test pins a state Model B has disowned. | Delete; CR-025 §S6 replaces it with `~/.agents/agents/` sandbox proofs. **CR-MDB-025** (§S0 question "which test pins are §D14 amendments"). |
| `tests/test_installer_assets.py:64,164-215` | real-home superset check | **M** | `ImportedSkillBundleFidelityTest` requires `~/.claude/skills/<name>` to exist for 6 bundles and the repo bundle to be a superset of the deployed file set. A fresh clone / CI fails; on this machine it passes through the `~/.claude/skills → ~/.agents/skills` symlinks, i.e. it now measures the installer's own output against itself. | Delete (the sandboxed `DeployEngineSevenBundlesEndToEndTest` in the same file already proves deploy fidelity). **UNOWNED**. |
| `tests/test_installer.py:48-91` · `test_scaffold.py:42-92` · `test_hooks.py:108-151` | real-home mtime guards | **L** | `setUpModule`/`tearDownModule` snapshot mtimes of the real `~/.claude/skills`, `~/.agents` (recursively — the whole store) and `~/.claude/hooks`; any concurrent harness write (Pi writing `~/.agents/*`) raises from `tearDownModule` → an ERROR for the whole module, unrelated to the code. Also read-only real-home coupling in modules that otherwise sandbox correctly. | Keep the intent, but guard only `~/.agents` roots the deploy engine actually writes, or drop now that every deploy test pins `--target-root`. **UNOWNED**. |
| `tests/test_agent_generator.py:250-291` | mutates a tracked repo file | **L** | `test_s3_check_flag_exits_one_and_names_the_mutated_file` appends to `generator/agents/python-red-agent.md` in place (restored in `finally`). A crash mid-test leaves the working tree dirty; `BespokeUntouchedS4Test` (same file, `:497-535`) already shows the isolated-tmp-repo pattern. | Move the mutation into the tmp repo fixture. **UNOWNED**. |

### 1c. chezmoi introspection — the eight gates (CR-MDB-021) plus one it missed

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `test_core_split.py:254-294` · `test_cr_authoring_skill.py:231-259` · `test_crucible_skill.py:329-356` · `test_git_chezmoi_skills.py:272-299` · `test_memory_model.py:281-308` · `test_model_b_skill.py:232-262` | `chezmoi diff` gates (×6, byte-identical bodies) | **H** | All six **FAIL today** (see §3). `chezmoi diff` defaults `--recursive=false`, so 3 of the 5 path args are vacuous (README 2026-08-27 (i)); the two file args compare the user's dotfile source against the user's home. The docstring "EXACT bound — a clean exit is required" at `:286` is the only non-vacuous part and it is what now fails. | Remove. **CR-MDB-021** (filed; lists exactly these six + the two below). |
| `test_core_split.py:296-364` | `chezmoi apply --dry-run` gate | **H** | Writes `/tmp/claude-1000/chezmoi-noauto.toml` derived from the user's live `~/.config/chezmoi/chezmoi.toml` (side effect outside the sandbox), then runs `chezmoi apply --dry-run` against the real home. | Remove. **CR-MDB-021**. |
| `test_realhome_supersede.py:391-419` | `chezmoi status` precondition | **M** | Gated behind `MODELB_REALHOME_GATE`; shells `chezmoi status`. | Remove. **CR-MDB-021**. |
| `test_installer_assets.py:445-456` | grep gate `chezmoi` under `generator/` | **L** | Not one of the eight; it is a *negative* gate (zero `chezmoi` refs) and is correct under the policy — keep. | No action. |
| `test_git_chezmoi_skills.py:151-233` | pins the deployed **chezmoi skill** content | **M** | `ChezmoiSkillS3Test` asserts `~/.claude/skills/chezmoi/SKILL.md` contains `chezmoi destroy`, `autoCommit`, `never push` and the "`diff <dir>` silent-empty quirk" (`:214-217`). README 2026-08-27 measured that quirk as "not a bug but `--recursive=false`" — the test pins a claim already retracted, in a deployed file, for a tool Model B no longer depends on. `test_installer_assets.py:84-91` also counts `chezmoi` among the 6 owned bundles. | Decide whether the `chezmoi` bundle survives CR-021 ("Model B carries no chezmoi dependency"); if it stays as a user-facing skill, retarget the test to `skills-src/chezmoi/SKILL.md` and drop the quirk pin. **CR-MDB-021** (needs a scope line). |

### 1d. Pins on RETIRED Crucible / harness contracts (will need inversion)

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `tests/test_crucible_skill.py:169` | `--phase` positively pinned | **H** | `required_terms = [..., "--phase", ...]` — `--phase` was renamed `--role` fleet-wide (CR-CRU-059); the repo SKILL.md passes only because it says "renamed from `--phase`". README 2026-08-27 already names this line. | Invert to `--role` + `--cycle`. **CR-MDB-017**. |
| `tests/test_model_b_skill.py:132` | `--orchestrator` positively pinned | **H** | `required_terms` includes `"--orchestrator"`; `plan-file --orchestrator` was REMOVED in 0.1.0 and `--cycles "a,b"` refused in 0.2.2 (README 2026-08-27, 2026-09-18). `skills-src/model-b/SKILL.md:47` still teaches both and this gate *forces* it to. | Invert to `--agent` / repeated `--cycle --cycle-kind`. **CR-MDB-017** ("taught wrongly in four surfaces"). |
| `tests/test_crucible_skill.py:420-438` · `tests/test_contracts.py:79-91` · `tests/test_installer_assets.py:307-320` | `no-active-cycle` / `withhold` / `resolve_attach_cycle` positively pinned | **H** | Three modules require the "server-resolved cycle attach / no-active-cycle withhold" wording. The deployed `~/.agents/skills/crucible/SKILL.md` (measured 2026-09-07) records that mechanism as **DELETED by CR-CRU-056** — attachment is an explicit `register --cycle` binding. The repo copy keeps the words only in a "is DELETED" sentence, so the pins pass by accident. | Invert to negative pins (zero live mentions outside a retirement note). **CR-MDB-017** (§S1 client-verb sync) + **CR-MDB-009**'s `contracts/crucible-envelope.md` via 017. |
| `tests/test_hooks.py:50-66,434,448,475,492,497-503,528` | STATUS-CONTRACT **1.0.0** shape, `lastRunCr` | **M** | Fixtures and assertions pin document version 1.0.0 and the `lastRunCr` field; Crucible's contract is 2.0.0 and the field was renamed `lastClosedCr` (CR-CRU-094 §S4, "clean break, no alias"). | Re-pin from the manifest's `status` key. **CR-MDB-019**. |
| `tests/test_agent_generator.py:96-116,334-338,594-600` · `tests/test_installer_assets.py:97-98` | 4-stack × 13-bespoke census | **M** | `STACKS = [arduino,bun,python,quarkus]`, `BESPOKE_AGENT_NAMES` = rust×4 + vscode×4 + electronics×4 + inbox-analyst. CR-024 makes rust a generated stack (bespoke → 9, targets → 20 + codec; `:574-580` hard-codes "exactly 17 written files"). | Amend counts. **CR-MDB-024**. |
| `tests/test_agent_generator.py:365-447` · `tests/test_installer_assets.py:323-360` | frontmatter/body pins on the Claude Code shape | **M** | Content checks compare `generator/agents/*.md` byte-for-byte against `render()` and require `name:`/`description:`/`sub-agent-procedure` citation. Generated files today carry `effort/color/maxTurns/skills:` (Claude Code keys, none in Pi's contract) and cite `~/.claude/skills/model-b/references/sub-agent-procedure.md` (`python-red-agent.md:15`). The tests do not pin those keys explicitly, but they freeze whatever `render()` emits, so the CR-025 rewrite must regenerate and re-run them in one wave. | No inversion needed; note in CR-025 gap-analysis. **CR-MDB-025**. |
| `tests/test_crucible_skill.py:498-522` · `tests/test_hooks.py:258` | `.claude/scripts` negative gates | ok | These are correct negative gates (zero mentions). Keep. | — |

### 1e. Vacuous / self-defeating gates

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `tests/test_crucible_skill.py:486-496` | self-defeating grep gate | **H** (1 of 7 current failures) | AC7 asserts zero `WORKFLOW_CYCLE_ID` in repo `AGENTS.md`. The only two occurrences (`AGENTS.md:135,138`) are the Testing & QA section *describing this very gate and its failure*. The 2026-08-27 process note records the class: "a spec cannot forbid a string and also document the prohibition". Baseline note calls it "pre-existing and unrelated" — it is a gate-design defect, not drift. | Scope the gate to the wrapper sentence (or the `## Testing` section excluded), or document with the split-literal idiom `test_toon_codec.py:122` uses. **UNOWNED** (CR-012 verification suite is the natural home). |
| `tests/test_cr_authoring_skill.py:281-291` | silent skip in loop | **L** | `test_s4_named_consumer_surfaces…` `continue`s when a named surface is missing ("Skip silently here"); 4 of the 5 surfaces (`memory/QUICK_REFERENCE.md`, `chezmoi-integration.md`, …) were *deleted* by CR-004/006, so the loop checks only `AGENTS.md`. | Drop the deleted surfaces from `CONSUMER_SURFACES`. **UNOWNED**. |
| `tests/test_realhome_supersede.py` (whole module, 10 tests) | env-gated → silently green | **M** | Skips unless `MODELB_REALHOME_GATE=1`; `test_tooling_detachment.py:947-988` even *requires* it stay gated. Run with the gate on today: **1 failure** — `test_deployed_skill_md_documents_v2_heartbeat_form…` because the deployed `~/.agents/skills/crucible/SKILL.md` has been hand-edited ahead of the repo (no `/api/v2/agents/heartbeat`, cites the dev checkout path, documents CR-CRU-056/059/094). The suite's "green" hides a real repo-vs-deployed divergence. | Either retire the module (installer sha256 manifest already guarantees deploy integrity — README 2026-08-27) or make it a documented operational check, not a suite member. **UNOWNED** (CR-021's rationale extends to it). |
| `tests/test_tooling_detachment.py:947-988` | test-of-a-test | **L** | AST-parses `test_realhome_supersede.py` to assert every class carries `skipUnless(GATE_ENABLED…)` and names the eight tools. Couples two modules by source text; it will fail the moment the real-home module is retired. | Delete alongside the real-home module. **UNOWNED**. |
| `tests/test_worktree_flow_axi.py:193-204` | vacuity guard on real home | **L** | "guards against vacuous pass" by asserting ≥1 `~/.claude/skills/*/SKILL.md` mentions worktree-flow — a real-home count. | Retarget to `skills-src/`. **UNOWNED** (see 1b). |

### 1f. Duplicated gates that will drift

| Pattern | Copies | Sev | Action |
|---|---|---|---|
| `chezmoi diff` on `CHEZMOI_SCOPE_PATHS` — identical 25-line body | ×6 (`test_core_split:254`, `test_cr_authoring_skill:231`, `test_crucible_skill:329`, `test_git_chezmoi_skills:272`, `test_memory_model:281`, `test_model_b_skill:232`) | H | Removed wholesale by CR-021. |
| `_split_frontmatter`, `_read`, `_files_under`, `_files_containing`, `_archive_has_content_move` helpers | ×5–7 modules each (`test_core_split`, `test_cr_authoring_skill`, `test_crucible_skill`, `test_git_chezmoi_skills`, `test_memory_model`, `test_model_b_skill`, `test_agent_generator`, `test_skills_handover`) | L | Hoist into `tests/_helpers.py` (the package has `__init__.py`). UNOWNED. |
| `build.py --list` exact target-set pin (16 agents + codec) | ×2 (`test_agent_generator.py:293-338`, `test_installer_assets.py:363-429`) | M | Two copies of the same contract; CR-024 must amend both. Keep one. CR-MDB-024. |
| `build.py --check` exits 0 | ×3 (`test_agent_generator:229`, `test_installer_assets:431`, `test_toon_codec:567`) | L | Keep one. UNOWNED. |
| `_run_module` / `_write_fake_executable` / `_FAKE_UV_SCRIPT` / mtime guard | ×3–4 (`test_installer`, `test_installer_assets`, `test_scaffold`, `test_tooling_adoption`, `test_hooks`) | L | Hoist. UNOWNED. |
| `/api/v2/agents/heartbeat` v2-form composed-pattern check | ×3 (`test_crucible_skill:139-152`, `test_skills_handover:280-303,396-415`, `test_realhome_supersede:239-278`) | L | Keep the `skills-src`-wide one in `test_skills_handover`. UNOWNED. |
| Sandboxed installer e2e with `--harnesses claude-code` | ×4 (`test_installer:898`, `test_installer_assets:483,579,743`, `test_tooling_adoption:638`) | M | See §2 — all four hard-wire the dropped harness. |
| `WORKFLOW_CYCLE_ID == 0` gates | ×6 (`test_crucible_skill:396,486`, `test_contracts:107`, `test_installer_assets:313`, `test_skills_handover:210`, `test_scaffold:513`, `test_hooks:256`) | L | One repo-wide gate would replace all (CR-012's "permanent regression guard" intent). UNOWNED. |

### 1g. Naming-convention violations (`<Topic><Section>Test` / `test_s<n>_…`)

| Module | Classes without a section token | Methods without `test_s<n>_`/`test_ac<n>_` |
|---|---|---|
| `test_installer.py` | 9 of 9 | 23 |
| `test_scaffold.py` | 13 of 13 | 25 |
| `test_hooks.py` | 6 of 6 | 14 |
| `test_hooks_compiler.py` | 7 of 7 | 13 |
| `test_skills_handover.py` | 7 of 7 | 11 |
| `test_installer_assets.py` | 9 of 9 | 10 |
| `test_realhome_supersede.py` | 4 of 4 | 10 |
| `test_worktree_flow_axi.py` | 3 of 3 | 6 |
| `test_agent_generator.py` | 1 (`DeployedAgentsConsumerConstraintTest`) | 0 |
| `test_crucible_skill.py` | 1 (`CrucibleSkillCRMDB011Test`) | 0 |

**Total: 60 of ~80 classes and 112 of 240 methods** ignore the convention AGENTS.md states. Sev **L**; the convention is documentary, but AGENTS.md should either be amended to "wave-3+ modules use `<Feature>Test` / descriptive names" or the rule dropped. UNOWNED.

## 2. Unwanted — guarding dropped targets / residue / third-party

| Location | Category | Sev | Evidence | Action / owner |
|---|---|---|---|---|
| `tests/test_hooks_compiler.py:161-215` (`ClaudeCodeEmitterTest`), `:475-504` (`RefusalTest` claude-code half), `:442-472` ("all four harnesses") | Claude Code emitter | **M** | 3 tests exist solely to prove `.claude/settings.json` wiring and the claude-code refusal rule. §D14 drops Claude Code; CR-025 §S5 explicitly freezes "roster/hooks UNCHANGED", so these are *deferred*, not deletable this wave. | Retire with the roster trim after CR-025 (candidate CR-029 scope or a new "harness roster = pi" CR). **UNOWNED**. |
| `tests/test_installer.py:263,285,308,357,411,443,462,506,589,649,881` · `test_installer_assets.py:485,621,745` · `test_tooling_adoption.py:675` · `test_scaffold.py:111,335,423,720,868,979,1003,1045,1099` | `--harnesses claude-code` as the *only* exercised harness | **M** (pattern ×23 sites) | Every installer/scaffold e2e selects `claude-code` and asserts `.claude/skills/<name>` symlinks and `.claude/settings.json`. Not one e2e exercises `--harnesses pi`. The PI-only target is untested end-to-end at the installer level. | Flip the default fixture harness to `pi` (asserting `.pi/extensions/*.ts` and the `~/.agents` store); keep one claude-code case only until the roster trims. **CR-MDB-025** (§S5/§S6 installer proofs) then roster-trim CR. |
| `tests/test_installer.py:710,746-845` | roster `["claude-code","hermes","pi","opencode"]` | **M** | `HarnessTargetingTest` pins the 4-harness roster and detection of a `claude` binary. | Amend with the roster trim. **UNOWNED**. |
| `tests/test_scaffold.py:495-527,955-993` | `CLAUDE.md` symlink anchor + "hermes/pi.dev/opencode" native-anchor notes | **M** | Scaffold must emit `CLAUDE.md → AGENTS.md` for claude-code and *must not* mention `pi.dev` when only claude-code is installed. | Amend with the roster trim. **UNOWNED**. |
| `tests/test_scaffold.py:802-853` | `.claude/settings.json` hook wiring from `init` | **M** | Solo/rust scaffold proof reads `.claude/settings.json`; the Pi equivalent (`.pi/extensions/*.ts`, `test_hooks_compiler.py:280-393`) has no scaffold-level test. | Add a Pi scaffold proof; retire the claude-code one with the roster. **CR-MDB-025** / roster-trim. |
| `tests/test_tooling_adoption.py:496-523` | pins `HARNESS_SKILL_DIRS == {"claude-code": ".claude/skills"}` | **M** | Asserts the per-harness symlink map has exactly one key, `claude-code`. Under Pi-only, Pi reads `~/.agents/skills` directly (no symlink) — the map should become empty or `pi`-keyed. | Amend with the roster trim. **UNOWNED**. |
| `tests/test_agent_generator.py:83,603-625` | `~/.claude/agents` consumer constraint | **M** | Duplicate of 1b — a real-home Claude Code guard. | Delete. **CR-MDB-025**. |
| `tests/test_git_chezmoi_skills.py:151-233` · `test_installer_assets.py:89` | chezmoi skill bundle | **M** | See 1c. | **CR-MDB-021** scope decision. |
| `tests/test_agent_generator.py:112-115` | electronics names | ok | Appear only in the *never-write* list, which is exactly §D3's rule. Not residue. | Keep. |
| Third-party imports | — | ok | Import census over all 19 modules: `unittest, pathlib, subprocess, os, tempfile, sys, shutil, tomllib, re, json, importlib.util, ast, hashlib, zipfile, time, stat, modelb_axi`. **Zero third-party packages.** The `build` frontend is probed via `find_spec` (`test_tooling_detachment.py:1154`) and skipped, not imported. | — |

## 3. Current failures & skips — environmental vs real

**7 failures**

| Test | Verdict | Evidence |
|---|---|---|
| `test_core_split…test_s5_chezmoi_diff_clean_on_cr_touched_paths` + the 5 sibling `test_s{3,4}_chezmoi_diff_clean…` (`test_cr_authoring_skill`, `test_crucible_skill`, `test_git_chezmoi_skills`, `test_memory_model`, `test_model_b_skill`) | **ENVIRONMENTAL** (user dotfile drift) | Diff output: `~/.claude/AGENTS.md` mode `100600→100644` and the "Read `~/.claude/skills/model-b/references/sub-agent-procedure.md`" block removed from the live file vs the chezmoi source. Nothing in this repo produces or consumes that file; the gates compare the user's dotfile source to the user's home. This is the false-green→false-red flap CR-021 predicted ("what they compare … is not a Model B property"). AGENTS.md's "240/1F" baseline was measured when the user's tree happened to converge. **Fix = CR-MDB-021 removal**, not a repo change. |
| `test_crucible_skill.CrucibleSkillCRMDB011Test.test_ac7_repo_agents_md_no_longer_claims_workflow_cycle_id_injection` | **REAL, but a gate-design defect** | `AGENTS.md:135,138` contain the literal `WORKFLOW_CYCLE_ID` only inside the Testing & QA prose that documents this gate and its baseline failure. The wrapper-sentence claim the AC targets is gone. The test forbids the string the doc must spell to describe it. Fix the gate scope (or use the split-literal idiom); no product content is wrong. |

**12 skips**

| Skips | Verdict | Evidence |
|---|---|---|
| 10 × `test_realhome_supersede.*` | **BY DESIGN, but masking** | Env-gated. Run with `MODELB_REALHOME_GATE=1`: 9 pass, **1 fails** (`test_deployed_skill_md_documents_v2_heartbeat_form…` — `~/.agents/skills/crucible/SKILL.md` lacks `/api/v2/agents/heartbeat`; the deployed copy has been edited ahead of `skills-src/`, cites the dev-checkout path and CR-CRU-056/059/094). The suite's green hides a real repo/deployed divergence. |
| `test_skills_handover.ImportedBundleByteIdentityTest` · `test_installer_assets.CrucibleHandoverBundleFidelityTest.test_each_handover_bundle_exists…` | **PERMANENT (dead tests)** | Both skip on `~/Documents/data_projects/crucible/clients/skills` absent; the origin retired after CR-016 and the path is now out of bounds anyway. They can never run again. |

**Latent environmental dependencies that pass today only because of this machine** (would fail/skip elsewhere): `test_scaffold.py` ×3 (dev-checkout `toon.py`, hard fail), `test_hooks.py` ×3 (dev-checkout `toon.py`, hard fail), `test_toon_codec.py` ×2 + `test_tooling_detachment.py` ×1 (dev-checkout oracle, skip), `test_installer_assets.ImportedSkillBundleFidelityTest` (real `~/.claude/skills`, hard fail), `test_worktree_flow_axi.py` ×6 (real `~/.claude/scripts` + `~/.claude/skills`, hard fail), `test_agent_generator.DeployedAgentsConsumerConstraintTest` (real `~/.claude/agents`, hard fail), all of §1b's wave-1/2 modules (~30 methods, hard fail), and 2 `uv tool install` probes (`test_installer.py:196`, `test_installer_assets.py:579`) that need `uv` + a resolvable `hatchling` (network or cache).

## Summary

1. The suite is **240/7F/12S, not the 240/1F/12S AGENTS.md claims**; 6 of the 7 failures are the CR-021 chezmoi gates flapping on user dotfile drift, the 7th is a self-defeating grep gate in `test_crucible_skill.py:486`.
2. **Ten sites in 6 modules reach into the forbidden Crucible dev checkout** (`test_scaffold`, `test_hooks`, `test_toon_codec`, `test_tooling_detachment`, `test_skills_handover`, `test_installer_assets`); three of them hard-fail off this machine and the AC6 AST gate meant to catch this cannot see `spec_from_file_location`.
3. **Eight modules assert real `~/.claude`/`~/.agents` state** (wave-1/2 gates, `test_worktree_flow_axi`, `test_agent_generator`, `test_installer_assets`), contradicting the repo-local authoring rule; `test_worktree_flow_axi` drives stale chezmoi-managed copies and is fully superseded by `test_toon_codec`.
4. **Positive pins on retired Crucible contracts** (`--phase` :169, `--orchestrator` :132, `no-active-cycle`/`resolve_attach_cycle` in 3 modules, STATUS-CONTRACT 1.0.0/`lastRunCr`) must be inverted by CR-017/019 before those CRs' GREEN can pass; **no e2e exercises `--harnesses pi`** — 23 sites hard-wire the dropped `claude-code`.
5. Hygiene: 60 classes / 112 methods off the stated naming convention, ~8 helper functions copy-pasted across 5–7 modules, and no third-party imports anywhere (good).

**Top 3**

1. **`tests/test_scaffold.py:51` + `tests/test_hooks.py:415` — dev-checkout `toon.py` imported in-process** (6 tests, hard-fail elsewhere, violates the 2026-09-18 rule, and slips the CR-022 AC6 gate). One-line fix each: `from modelb_axi import toon`. UNOWNED — file it.
2. **`tests/test_worktree_flow_axi.py` (whole module) — exercises `~/.claude/scripts/*` stale copies, never the repo's `scripts/worktree-flow.py`**, and pins the retired `DEPLOYED COPY` banner. Delete the two superseded classes, retarget the third to `skills-src/`. UNOWNED (deferred register 2026-08-27 items (i)/(ii)).
3. **Retired-contract positive pins blocking CR-017/019**: `test_crucible_skill.py:169` (`--phase`), `test_model_b_skill.py:132` (`--orchestrator`), `test_crucible_skill.py:420-438` / `test_contracts.py:79-91` / `test_installer_assets.py:307-320` (`no-active-cycle` withhold, deleted by CR-CRU-056), `test_hooks.py:475-503` (`lastRunCr`, 1.0.0). Invert as part of 017 §S1 and 019.
