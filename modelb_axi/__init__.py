"""modelb-axi — Model B universal installer + scaffold entrypoint.

Adaptive TUI per DN-scaffold-packaging: launched with no ``install.toml``
under ``$MODELB_HOME`` it runs the installer flow; with one present it
hands over to the scaffold flow (CR-MDB-013).
"""

__version__ = "0.1.0.dev0"
