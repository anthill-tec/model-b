# Model B — CR queue

**Project:** Model B (acronym: MDB · solo orchestrator: `vidushi-mdb`) · **Crucible projectKey:** `019f7eb8-8cad-7000-9838-854eca8e7c20` · **Design contract:** `docs/research/PRD-model-b-rationalization.md` · **Evidence base:** `audits/2026-07-20-*.md` + `docs/research/DN-rationalization-plan-review.md` · **Ontology:** `crucible:docs/research/DN-model-b-language.md` · **Target release:** 1.0.0
Conventions (naming, agentIds, workflow rules) live in the project `AGENTS.md` — not here.

Queue rows enumerate the whole delivery (structure only). **Live status lives on the Crucible board** (plans/cycles/milestones — we file every run and transition there); `Type` lives in each spec's front matter. Pick the next CR by wave + `Depends on` against the board. Spec files are authored at wave-open.

## Queue

| CR | Title | Wave | Depends on |
|---|---|---|---|
| [CR-MDB-001](CR-MDB-001-core-split.md) | Core split: frugal AGENTS.md + procedure relocation + shim removal | 1 | — |
| [CR-MDB-002](CR-MDB-002-model-b-skill.md) | model-b skill body: orchestration trio + sandesh + canonical definition + universal conventions (PRD §D3) | 2 | 001 |
| [CR-MDB-003](CR-MDB-003-crucible-skill-rewrite.md) | crucible skill rewrite: real client surfaces, absorb report skills + agent-protocol | 2 | 001 |
| [CR-MDB-004](CR-MDB-004-cr-authoring-skill.md) | cr-authoring skill: cr-prd-dn-conventions + project-management split | 2 | 001 |
| [CR-MDB-005](CR-MDB-005-git-chezmoi-skills.md) | git-workflow + chezmoi skills: memory-twin merges + delete/rename procedure | 2 | 001 |
| [CR-MDB-006](CR-MDB-006-memory-model.md) | Memory model: global→project-level migration (D5) — only language refs stay global; merges + deletions | 2 | 002, 003, 004, 005 |
| [CR-MDB-007](README.md#footer-notes) | "Plan B" → "Model B" naming sweep across skills + memory | 2 | 002 |
| [CR-MDB-008](CR-MDB-008-agent-generator.md) | Agent generator: role templates + stack params + build.py --check gate; regenerate 16 agents | 3 | 002, 006 |
| [CR-MDB-009](CR-MDB-009-contracts.md) | contracts/: crucible-envelope mirror, sandesh-cli, mail-axi, lean-ctx specs | 3 | — |
| [CR-MDB-010](CR-MDB-010-worktree-flow-axi.md) | worktree-flow.py AXI output: TOON envelope for status/next/finish + lane boards | 3 | 009 |
| [CR-MDB-011](CR-MDB-011-crucible-skill-integration.md) | crucible skill integration: route to Crucible-bundled skill docs (final-contract sync) | 3 | 003, 009 |
| [CR-MDB-013](CR-MDB-013-scaffold-cli.md) | Model B scaffold flow (`modelb-axi` TUI): project initializer per PRD §D10 | 3 | 002, 008, 014 |
| [CR-MDB-014](CR-MDB-014-universal-installer.md) | Universal installer flow + packaging (uv/pipx, adaptive TUI, dependency orchestration) per PRD §D10 | 3 | 002, 008 |
| [CR-MDB-015](CR-MDB-015-harness-agnostic-hooks.md) | Harness-agnostic hooks: neutral schema + per-harness emitters + shared script protocol (DN-harness-agnostic-hooks) | 3 | 013, 014 |
| [CR-MDB-016](CR-MDB-016-crucible-skills-handover.md) | Crucible skills handover: adopt clients/skills/ as canonical, bundle to published artifact, supersede stale deployed skill | 4 | 011, 014 |
| [CR-MDB-022](CR-MDB-022-tooling-adoption.md) | Tooling adoption (WAVE 5 FIRST): eight workflow scripts into `scripts/`, one Model B-owned TOON codec, bundled + installer-deployed, every reference detached from the local machine | 5 | — |
| [CR-MDB-023](CR-MDB-023-code-health-skill-adoption.md) | `code-health` skill adopted as a published bundle: detached tool paths, recorded drift closed, bundle accounting corrected | 5 | 022 |
| [CR-MDB-017](CR-MDB-017-client-role-contract-sync.md) | Client-verb contract sync: `--role` + mandatory `--cycle` binding replace the retired `--phase` across the seven owned bundles | 5 | 016 |
| [CR-MDB-018](CR-MDB-018-crucible-discovery-capture.md) | Crucible discovery capture: probe `crucible-axi`, read the client manifest, persist `[install].clients_dir` | 5 | 014, 015 |
| [CR-MDB-019](CR-MDB-019-hook-runtime-correctness.md) | Hook runtime correctness: status-contract-document re-pin, arduino stack marker, honestly-async opencode emitter | 5 | 015, 018 |
| [CR-MDB-020](CR-MDB-020-client-path-anchoring.md) | Client-path anchoring: every client reference resolves to Crucible's published contract; Model B maintains none of their clients | 5 | 017, 022 |
| [CR-MDB-024](CR-MDB-024-rust-stack-adoption.md) | Rust as a fifth generated stack: close the orphaned `~/.claude/agents/rust-*` gap CR-CRU-042 assigns to Model B | 5 | 017 |
| [CR-MDB-021](CR-MDB-021-chezmoi-retirement.md) | Retire chezmoi introspection from the test suite: eight false-green release gates removed, policy self-enforcing | 5 | — |
| [CR-MDB-026](CR-MDB-026-watcher-launch-supervision.md) | The wake watcher must stay alive: supervised process + `restart: on-failure`, and the three-exit taxonomy (mail / timeout / lock-conflict) the bundles conflate | 5 | — |
| [CR-MDB-025](CR-MDB-025-omp-harness-support.md) | OMP as a first-class deploy target: neutral agent-definition schema + per-harness emitters, agent-defs as an installer asset class, OMP hook emitter, `omp` in the roster | 5 | 017 |
| [CR-MDB-012](CR-MDB-012-release-1.0.0.md) | Release 1.0.0: full verification suite (PRD §4) + archive/mapping.md + master tag | 5 | 006, 007, 008, 010, 011, 013, 014, 016, 017, 018, 019, 020, 021, 022, 023, 024, 025, 026 |

**— v1.0.0 ships here —** (release CR bundles the final gates; no close-out wave)

## Setup tasks (pre-wave — not a wave; a wave is a grouping of CRs)

- [x] `git init` + `git flow init` (master/develop; on develop) — 2026-07-20
- [x] Workspace scaffold — 2026-07-20
- [x] Persist the three audit reports into `audits/` — 2026-07-20
- [x] Register project in Crucible (`Model B`, key above) — 2026-07-20
- [x] Record chezmoi source baseline commit → `archive/BASELINE.md` — 2026-07-20
- [x] Prove chezmoi delete round-trip on a scratch file — PASSED 2026-07-20 (see `archive/BASELINE.md`)

## External dependency (Wave 3)

ALL `*-crucible.py` client implementation is CRUCIBLE's (requested #1325; answered #1326). DELIVERED (#1330, 2026-07-21): CR-CRU-030 merged — fleet-wide TOON-AXI conversion (bun/python/rust/mvn/arduino) via shared `_crucible_axi.py` + `toon.py`; streaming `pre-merge-gate`. DELIVERED (#1332, 2026-07-22): CR-CRU-036 merged (crucible develop `949a2f4`) — `WORKFLOW_CYCLE_ID` removed (server-resolved active cycle, warn+withhold on none), fleet coverage gap closed (rust/mvn/arduino `pre-merge-gate` + `regression --coverage`), per-stack skill docs BUNDLED at `crucible:clients/skills/crucible-report-*/` (managed + updated by Crucible). The external dependency is CLOSED; NEW vscode client still its own unscheduled CR on their side. CR-MDB-011 is RESCOPED to integrate + route to the bundled skill docs (never re-document their surfaces).

## Footer notes

- 2026-07-20 — Queue opened; wave plan + 11 design decisions locked via lavish review; PRD filed. Structural waves (1–3) run with no other live orchestrator sessions.
- 2026-07-20 — Crucible-client ownership corrected: never Model B's; requested of Crucible (#1325 superseding erroneous #1324); their scheduling answer recorded (#1326).
- 2026-07-20 — Naming standard set (header). CR-MB-001 → CR-MDB-001 (spec, branch, Crucible plan 15; plan 14 superseded, both plans now closed at merge `4fd2fca`).
- 2026-07-20 — CR-MDB-001 shipped: RED 3/11/14 → GREEN 14/14 → VERIFY(FIX_REQUIRED→APPROVE) → regression 14/14; core 621→42 lines; chezmoi source `b89f936`+`efe71e6`.
- 2026-07-20 — Full CR decomposition enumerated in the queue (user directive: the queue lists the whole delivery with dependency edges upfront; spec files still authored at wave-open). `plans/` folder removed — the standard PRD/CR model under `docs/` is the delivery model; the lavish-reviewed plan record moved to `docs/research/DN-rationalization-plan-review.md`.
- 2026-07-20 — Waves 3 and 4 COMBINED into wave 3 (user directive via lavish): generator + AXI tooling + contracts ship together. There is NO close-out wave — delivery ends in a standard git-flow RELEASE (verification suite + archive/mapping.md ride the release; master tagged).
- 2026-07-20 — ELECTRONICS EXCLUDED (user): anthill-forge dead/deprecated; hw-crucible.py shim + 4 electronics agents + electronics skills under revision — ignored this delivery (PRD §D7 note). Wave-2/3 CRs must not touch them.
- 2026-07-20 — CR-MDB-007 SUPERSEDED (absorbed by 002–006): exact-phrase "Plan B" grep over skills/ memory/ AGENTS.md agents/ hooks/ scripts/ returns ZERO hits — the sweep happened during the moves/deletions. Permanent zero-Plan-B regression guard rides CR-MDB-012's verification suite. WAVE 2 CLOSED (002–006 shipped, 007 superseded).
- 2026-07-20 — VERIFY CR-MDB-002 routed forward: rust-orchestration.md L19 dangling ref to deleted memory/sandesh.md → fold into CR-MDB-006; agent-protocol prose roughness dies with CR-MDB-003.
- 2026-07-20 — DEFERRED (next SCRUM): (a) chezmoi v2.71 `diff <dir>` silently empty for drifted children (dry-run gate covers recursion); (b) pre-existing SOURCE-AHEAD chezmoi drift on 6 agent defs + `orchestration-common/track.md`, `project-management.md` — reconcile deliberately, never blind apply; (c) `bun-fix-agent.md` 2-line source delta.
- 2026-07-21 — Wave 3 in flight: 009 (`2328f24`), 010 (`cd21fad`), 008 (`237512d`) shipped with APPROVE verdicts; regression 81/81. CR-MDB-013 start ruled PREMATURE (user): feature branch deleted, plan-27 cycles 21/22 skipped, undo milestone posted. Installer-vs-scaffold split adopted (user): the UNIVERSAL INSTALLER (014) deploys user-local files per targeted harness; the scaffold (013) is project-specific (stack + overrides), reading the installation config. Open questions Q1–Q6 in the lavish Setup-split section; 013 re-plan, 014 DN, and the 015 deployment boundary block on their settlement. 011 still awaiting Crucible intimation (#1322).
- 2026-07-21 — Crucible intimation #1330 ADOPTED (user): CR-CRU-030 merged (fleet TOON-AXI, shared `_crucible_axi.py`/`toon.py`, streaming pre-merge-gate) — 011's external blocker lifted but HELD until CR-CRU-036 (WORKFLOW_CYCLE_ID removal → server-resolved active cycle) for a single sync against the final contract. Installer boundary: Crucible ships its OWN installer (server+clients bundle); the Model B universal installer (014) ORCHESTRATES it — depends on + invokes, never mirrors clients (PRD D10). CR-CRU-035 seam: Crucible core scripts + status contract, Model B hook templates + generation (015). No WORKFLOW_CYCLE_ID in hook templates or installer; the /tmp wrapper's cycle-id injection dies at the 013 re-plan wrapper rewrite.
- 2026-07-22 — CR-CRU-036 DELIVERED (#1332, crucible develop `949a2f4`): client contract FINAL; external dependency closed. Setup design SETTLED via lavish (user): uv/pipx packaging (`uv tool install modelb-axi`), ONE adaptive TUI (installer flow ⟷ scaffold flow by detected ecosystem state), installer = DEPENDENCY ORCHESTRATOR (pre-flight installs Crucible/Sandesh/uv via their own methods), `$MODELB_HOME` per XDG/uv conventions, initial harness roster Claude Code · Hermes · pi (pi.dev) · OpenCode, **sequencing flipped 014 → 013** (queue deps updated: 013 +014, 014 −013, 015 +014), skills once-user-scope confirmed. 011 RESCOPED (integrate + route to Crucible-bundled skill docs) and re-titled. Open: Q5 upgrade story (folds into the 014 DN). User-confirmed execution: design sync → 011 → 014 spec amend + gap-analysis + re-plan → PAUSE + intimate before implementation. **REPO-LOCAL AUTHORING RULE (user, same day, emphatic): no CR writes `~/.claude` — artifacts are authored repo-local (`skills-src/` etc.), AC gates assert repo paths, deployment is EXCLUSIVELY the installer's (014). Binds 011/013/014/015/012; PRD D9 amended; 011 spec amended mid-flight (GREEN stopped, work salvaged to `skills-src/crucible/`, `~/.claude` restored byte-clean).**
- 2026-07-22 — CR-CRU-035 division COORDINATED (their #1333 → our #1334): hook creation + per-project deploy = Model B (015 generation + scaffold wiring + installer scripts); Crucible = READ-PATH ONLY — stable versioned `status` envelope contract (queue rows, active cycle, lastRunCr), tolerant+bounded for SessionStart use (fast, exit 0, warn-don't-block), client discovery via their future installer's machine-readable location; their client `setup`/hook-install verb DROPPED. CR-MDB-015 files against this division.
- 2026-07-23 — CR-MDB-015 SHIPPED (merge `c638617`) — **WAVE 3 CLOSED** (008/009/010/011/013/014/015 shipped, 012 release remains): C1 (schema v1 + seven protocol scripts — six stdlib ports contract-faithful + ambient-board-status per STATUS-CONTRACT 1.0.0) + C2 (hermes/pi researched → DN addendum; compile_wiring w/ four emitters + refusal rule) + C3 (scaffold seam fill, wheel + store deploy) → VERIFY FIX_REQUIRED (opencode fail-closed claimed-not-implemented; 3 unsourced pi event maps) → F1 (real spawn-failure block path; sourced-only pi map, pre-compact declared gap) → re-verify APPROVE → regression 173/173. Follow-ups: opencode async-vs-spawnSync style nit; Hermes revisit when project-level hooks ship.
- 2026-07-22 — CR-MDB-013 SHIPPED (merge `48e681d`): C1 (flow entry/flags/dry-run) + C2 (full emission, corrected .env.local check-ignore contract, §S5 REPO_OWNER propagation) → VERIFY APPROVE (independent artifact inspection) → F1 (honest fail-fast `--register`; 014 import-fidelity gate amended to durable coverage-superset contract) → regression 139/139. DEFERRED/follow-ups: interactive TUI prompting (v1 non-interactive-only), live `--register` implementation, Hermes/pi.dev harness config research (rides 015), python-specific memory template missing from asset set. Process note: a cross-identity test-amendment routing was correctly REFUSED by the 014-registered agent — re-routed to the active cycle's agent; identity discipline held.
- 2026-07-22 — CR-MDB-014 SHIPPED (merge `0e86ec9`): C1–C4 (package skeleton/adaptive TUI shell → pre-flight → targeting+deploy engine+config → imports+generator retarget) → VERIFY FIX_REQUIRED (real catch: assets never shipped in the wheel; dev-mode tests masked it) → F1 (wheel-embedded `modelb_axi/_assets`, installed-binary e2e) → re-verify APPROVE → regression 119/119. `modelb-axi` is uv-tool-installable with all seven skill bundles + generator assets packaged. ROUTED FORWARD: double `deps:` line labeling (013/015 consumer concern); stale `~/.claude/scripts` paths in inherited 008 generator content (repoint at deployment); real-home deploy activation deliberately deferred behind explicit `--target-root` — decide at/after 013.
- 2026-07-28 — DEFERRED (next SCRUM, from Crucible #1344): PORT the inherited 22-test semantic guard suite over the handed-over skill docs — lifted verbatim to `docs/research/crucible-clients-skills-guard.test.ts` (origin `df68bf5`, bun:test; their repo copy dies with CR-CRU-042). Guards: per-skill v2-endpoint truth, no unmarked v1 legacy, ingest-is-heartbeat semantics, zero WORKFLOW_CYCLE_ID per skill (their "keep this one whatever else you drop"), real client verbs in examples. Partial overlap with our AC2/AC3/AC8 gates; the endpoint-truth + real-verb guards are NEW and feed the maintenance-contract drift check. Candidate CR: python port into tests/.
- 2026-07-28 — CR-MDB-012 HELD (user): Model B's 0.1.0 waits for a PROPER CRUCIBLE RELEASE — the handover (016) ties our release content to their client surface (bundle docs + PRD §4 criterion 5 fleet smoke + the pre-release ask), so releasing against their moving develop is out. Unblock signal: Crucible release intimation on the #1336 thread (requested #1343); then 012 spec authoring starts with the pre-release ask against the RELEASED contract.
- 2026-07-28 — CR-MDB-016 SHIPPED (merge `7c116f6`) — WAVE 4 CLOSED: C1 (7-bundle byte-identical import from origin `74018ea` + CRUCIBLE-HANDOVER.md + crucible-skill routing/v2-touch sync + arduino router) → C2 (real-home supersede via installed modelb-axi: store+symlinks live, 6 script mirrors + 5 pycache residue retired, 25 referencers repointed/neutralized, skills tree de-chezmoi'd) → V1 VERIFY APPROVE (AC1–AC8 independent evidence) → F1 (docstring scope note + recursive gate) → regression 194/194. Six sanctioned test amendments (011 pins → superseding contract; bespoke gate re-mechanized repo-side per the user's no-chezmoi directive). Standing maintenance contract + pre-release ask recorded in CRUCIBLE-HANDOVER.md. agent-protocol stays absorbed (Option B); heartbeat.sh not adopted.
- 2026-07-28 — DEFERRED (next SCRUM, from CR-MDB-016): the imported bundle skill docs reference clients by UNANCHORED relative path (`clients/<stack>-crucible.py`) — resolvable inside a Model B project (project-layer binding: AGENTS.md client-path rule + wrapper) but a dangling edge for a global-skill-without-project session and across machines. Needs an anchor once Crucible's installer (their CR-CRU-009) ships machine-readable client-location discovery; candidate CR then.
- 2026-07-28 — CR-MDB-016 FILED (Wave 4, pre-release; user-approved option 1): Crucible's #1336 handover request ratified WIDENED — Model B owns the skills component FULLY (content + bundling + deploy; CR-CRU-035 division superseded); Crucible exits skills ([skills] installer stage removal on their side), their `clients/skills/` freezes after import. Answer sent #1337. Drives: 8-bundle import → skills-src/, wheel publish, supersede of the stale deployed `~/.claude/skills/crucible` (12 removed-API `WORKFLOW_CYCLE_ID` refs, no arduino — verified). Resolves the 014-deferred real-home deploy activation for the skills asset class. 012 gains dep 016.
- 2026-07-22 — CR-MDB-011 SHIPPED (merge `6aec41c`): RED 6+2 → GREEN 14/14+5/5 → VERIFY APPROVE → regression 89/89; `skills-src/crucible/` authored to the final 030+036 contract, contracts mirror DELIVERED, AGENTS.md wrapper sentence post-036. DEFERRED → 014 gap-analysis: (a) `generator/build.py` output target (chezmoi source → package assets); (b) 6 stale `~/.claude/scripts/*crucible*` copies + 23 agent defs / 2 hooks / 1 memory ref repoint + destroy at deployment; (c) AC8 test-coverage docstring cross-ref (VERIFY nit); (d) repo venv lacks `coverage` (no `app/` source — decide 012's coverage stance).
- 2026-08-27 — CR-MDB-012 HOLD LIFTED: Crucible shipped a proper release. Their tags are `0.1.0`, `0.1.1`, `0.1.2` (latest `0.1.2`, `package.json` agrees); intimated on the #1336 lineage as #1354 ("Crucible 0.1.0/0.1.1 shipped — client-fleet surface changes") and #1356 ("pin 0.1.1 now — 0.1.2 client-surface delta is NONE, install-path only"). The standing pre-release ask was re-issued against the RELEASED contract as #1357, with the discovery-manifest caveat as #1358. Version-axis discipline for every doc from here: Crucible's product version is `0.1.X`; "V2" is the `/api/v2/*` API generation; `clients/STATUS-CONTRACT.md`'s `2.0.0` is that one contract DOCUMENT's own semver. There is no Crucible 2.0.0.
- 2026-08-27 — WAVE 5 OPENED (patch wave, pre-release) — three CRs filed at the SCRUM against the released 0.1.X client surface. 017: the seven owned bundles still teach the retired `register --phase`; `--role` is required, `--cycle` is mandatory for RED/GREEN/FIX/VERIFY (409 unbound), and `--role`/`--cycle` appear zero times in the bundles today — plus `tests/test_crucible_skill.py:169` positively pins `--phase` and must be inverted. 018: `preflight.py:77` probes `crucible` but their installed CLI is `crucible-axi`, and `hooks-src/scripts/ambient-board-status:66` reads `[install].clients_dir` which `config.py` never writes — the ambient board feature has been dead on every install since 015; Crucible's CR-CRU-009 discovery manifest (`crucible-clients.json`) is the source that closes both. 019: the hook pins status-contract-document 1.0.0 (theirs is 2.0.0), `_STACK_MARKERS` cannot reach arduino, and the opencode emitter declares `async` over a blocking `spawnSync` whose failure predicate gates the fail-CLOSED return. 012 gains deps 017/018/019 and the wave integer its release ends.
- 2026-08-27 — Queue rows made MACHINE-READABLE (task, no CR). `queue-file` had registered ZERO rows: the client's parser requires the CR cell to be a Markdown link (`_crucible_axi.py:1898`), demands a leading integer in the Wave cell, and splits the Depends-on cell on commas. Our rows were plain text, CR-MDB-012's wave cell was `—`, and CR-MDB-011's dependency cell carried prose. Fixed all three; the external-dependency prose stays in its own section. CR-MDB-007 has no spec file (superseded, absorbed by 002–006) so its row links to these notes; CR-MDB-012's link is its spec-to-be, authored at release-open.
- 2026-08-27 — DEFERRED register RE-MEASURED at the SCRUM; three entries are now DEAD and are dropped: the chezmoi `diff <dir>` quirk is not a bug but `chezmoi diff`'s `--recursive=false` default (against `status`'s `--recursive=true`), and the SOURCE-AHEAD drift on the 6 agent defs / `orchestration-common.md` / `orchestration-track.md` / `project-management.md` and the `bun-fix-agent.md` 2-line delta have all converged — the tree is drift-free. What replaces them, still unfiled and awaiting a decision: (i) that recursion default makes 3 of the 5 path arguments VACUOUS in six release-gating `chezmoi diff` test methods (`tests/test_core_split.py:254`, `test_cr_authoring_skill.py:231`, `test_git_chezmoi_skills.py:272`, `test_memory_model.py:281`, `test_model_b_skill.py:232`, `test_crucible_skill.py:329`) — those gates are false-green; (ii) the 16 generated agent defs and `skills-src/chezmoi/SKILL.md` are STALE in the repo relative to their deployed copies, and `generator/stacks/*.toml` still emits the 016-retired `~/.claude/scripts/<stack>-crucible.py` path, so a `build.py build` + deploy would regress the deployment and break `test_realhome_supersede`'s retired-referencer gate; (iii) the 36 unanchored `clients/<stack>-crucible.py` references across 5 bundle SKILL.mds (2 mechanical phrasings, 1 sanctioned exclusion at `skills-src/crucible/SKILL.md:53`) plus the same retired-mirror citations in `skills-src/memory-templates/{java,rust}-orchestration.md` — UNBLOCKED, and the anchor form is now settled by #1360 (the manifest's `clients` values, packaged fallback in the interim, never site-packages and never `~/.claude/scripts`). The Hermes revisit STAYS deferred: nothing is wrong, the degradation is declared and tested, and it is gated on an upstream capability. The inherited guard suite is 19 static `test()` declarations / 37 runtime cases, not "22 tests", and 7 of those cases are unportable by design (they guard the ratified-out `agent-protocol` + `heartbeat.sh`).
- 2026-08-27 — CRUCIBLE ANSWERED the pre-release ask (#1359 for #1357, #1360 for #1358); 017/018/019 reconciled against it in the same sitting. **Latest release 0.1.2** (tag `9ef24b1`, 2026-08-19; PyPI `crucible-axi` 0.1.2, npm `@anthill-tec/crucible-server` 0.1.2 — npm has no 0.1.0, that publish died on provenance E422, so the npm line starts at 0.1.1). **The entire client-surface delta landed in 0.1.0**: `0.1.0..0.1.1` and `0.1.1..0.1.2` are both empty over `clients/` and `src/`, so our bundles have been stale since Crucible's FIRST release, not since a recent patch. 0.1.1 was a packaging hotfix; 0.1.2 is install-path only (`crucible-axi install [--target-dir <dir>]`, default `~/.crucible`; server version-pinned to the installer; run verb `crucible-axi serve`). **0.2.0 is NOT released** — no tag, absent from both registries — and must never be cited as shipped. Register contract confirmed with corrections we had wrong: `--cycle` is NOT argparse-required, the SERVER enforces it per role (409, verbatim `role RED requires a cycle binding — register with --cycle <cycleId>`), while a missing or out-of-enum `--role` is 400. Further 0.1.0 deltas we had not caught: `plan-file --orchestrator` REMOVED (our AGENTS.md still passes it), `gate-run --skip` added, `--source` added to rust/mvn/arduino. Two upstream traps recorded in 017 §S4: `cli/crucible-axi.ts register` has no `--cycle` at all (the released TypeScript CLI cannot register a TDD role), and `clients/rust-crucible.py`'s docstring register examples omit `--cycle` and are guaranteed 409s.
- 2026-08-27 — CAVEAT on the queue registration above: `queue-file`, the `queue` read verb and `POST /api/v2/projects/<key>/queue` are **develop-only, 0.2.0-bound — NOT in any Crucible release** (#1359). Our 19 rows registered because this project drives Crucible's develop checkout directly. The machine-readable row format is still correct and worth keeping, but no Model B doc may teach `queue-file` as available, and CR-MDB-012 must not assume board registration is reachable from a released client.
- 2026-08-27 — Discovery-manifest location SETTLED (#1360): the contract is `<target-dir>/clients/<stack>-crucible.py`, default `~/.crucible/clients/`, consumed via `crucible-clients.json`. Crucible CONFIRMED the defect this SCRUM found — no install stage materialises that directory today (`STAGE_ORDER = (server, manifest, unit)`), while the fleet rides in the wheel at `crucible_axi/clients/` for their own CLI's use. Ruling: anchor on the manifest, do NOT repoint at site-packages (their internal resolution detail, moves with the interpreter, not a stable surface), fall back to the packaged copy in the interim, and never resolve to the retired `~/.claude/scripts`. They file the materialisation fix at their next SCRUM; the manifest's shape will not change, so CR-MDB-018's consumption code survives it. CR-MDB-018 is therefore UNBLOCKED with a two-step resolution.
  - **SUPERSEDED 2026-08-27 (same day, user directive — see the note below):** the "fall back to the packaged copy in the interim" half of that ruling is WITHDRAWN. Crucible's suggestion was reasonable from their side, but adopting it would make Model B the maintainer of their client fleet. Resolution is the manifest or `unresolved`; the anchor-on-the-manifest and never-site-packages / never-`~/.claude/scripts` halves stand.
- 2026-08-27 — **USER DIRECTIVE: Model B carries no chezmoi dependency.** Local scripts and skills are not chezmoi-maintained; management would apply only to harness-installed skills, and is not required there either — the `modelb-axi` installer already guarantees integrity by sha256 manifest. This ratifies fleet-wide the policy CR-MDB-016 applied to one class only (`tests/test_agent_generator.py:35-46`). Filed as **CR-MDB-021**. Measured basis: the eight surviving gates all PASS today while proving almost nothing — `chezmoi diff` defaults `--recursive=false`, so the `agents/` (30 managed files) and `memory/` (6) directory arguments compare only the directory entries, and the `skills/` argument is structurally incapable of failing because every Model B-owned bundle is UNMANAGED (`chezmoi managed` lists only the user's own unrelated bundles). Only the two file arguments are live, and what they compare is the user's dotfile source against the user's home — not a Model B property. Removal loses no coverage: each of the seven `diff`/`apply` methods sits beside a sibling carrying the real deletion+archive AC, tabulated per module in the CR. Sanctioned amendment to closed CRs 001-006 + 016; suite count drops 194 → 187.
- 2026-08-27 — **USER DIRECTIVE: Model B maintains none of Crucible's client scripts.** That is the Crucible project's job; Model B manages and updates the SKILLS with reference to changes in Crucible. Two consequences, both filed. (a) **CR-MDB-020** — the anchoring CR that CR-MDB-017 designated at `:182`/`:190` but never filed — now covers both offending reference forms: 41 unrooted `clients/<stack>-crucible.py` occurrences across **10** files (5 report bundles + the 5 `skills-src/crucible/references/*.md` the earlier "36 across 5 files" count missed), and 12 references to the retired Model B-maintained mirror `~/.claude/scripts/<stack>-crucible.py` in `generator/stacks/*.toml` (`test_command`/`register_command`/`unregister_command` × 4 stacks) plus 2 in the java/rust memory templates. Skills cite Crucible's published contract (`<target-dir>/clients/`, default `~/.crucible/clients/`, discovered via `crucible-clients.json`) and name `crucible-axi install` as the way to satisfy it — no packaged-copy path, no site-packages path, no Model B substitute. `archive/` (44 occurrences) is immutable history and excluded by the gate. (b) **CR-MDB-018 AMENDED** — its packaged-copy fallback is REMOVED. Resolution is the manifest or `unresolved`+WARN; compensating for Crucible's un-materialised install stage would make Model B the maintainer of their fleet. Accepted consequence, stated in its Risk: `clients_dir` stays unresolved and the ambient board keeps its "no status feed resolved" degrade on every current install until Crucible ships materialisation. Its §S4 integration test now also covers the no-manifest degrade path.
- 2026-08-27 — **CR-MDB-017 WIDENED (§S6)** — the generator emits the retired flag at its source and was outside 017's original bundle-only scope. `--phase` is hard-coded in all four role templates (`red.md.tmpl:24`, `green.md.tmpl:29`, `verify.md.tmpl:26`, `fix.md.tmpl:23`) directly after `${register_command}`, NOT in the stack data; `--role` occurs in only 2 files anywhere under `generator/` and `--cycle` in none. All 16 generated agent definitions are therefore unable to register against a released Crucible — 400 for the missing role, 409 for the four TDD roles' missing cycle binding. §S6 fixes the templates and regenerates via `build.py build` with a clean `--check`. Ordering: 017 (verb) → 020 (paths); both rewrite the same five per-stack bundle SKILL.mds and both regenerate the 16 agents, so running 017 first means one regeneration settles both. CR-MDB-012 gains deps 020 + 021.
- 2026-08-27 — DEFERRED register CLEARED: all three items left unfiled at the 2026-08-27 SCRUM are now CRs — (i) the false-green `chezmoi diff` gates → CR-MDB-021; (ii) the generator's retired `~/.claude/scripts` emission → CR-MDB-020 §S2/§S3, and its stale-vs-deployed framing is RETIRED as a non-problem (the repo is the source of truth and the installer is the only deployment channel, so "stale relative to a deployed copy" describes the installer's job, not a defect); (iii) the unanchored client references → CR-MDB-020 §S1. The Hermes revisit remains the only standing deferral, gated on an upstream capability.
- 2026-08-27 — **USER DIRECTIVE: all Model B scripts are owned inside this subproject and repo-managed here; same-named copies in the Claude global directories are NOT Model B-managed, and this repo's version REPLACES them.** Filed as **CR-MDB-022**. The directive states the intent; the measured state was the opposite and is the reason the CR exists: **zero of the seven scripts exist anywhere in this repo** (`git ls-files scripts/` returns only `scripts/.gitkeep`), while `pyproject.toml:26` has force-included `"scripts" = "modelb_axi/_assets/scripts"` since CR-MDB-014 — so every wheel built since has shipped an EMPTY tooling directory, undetected because no gate asserts the asset class has content. Meanwhile seven published surfaces already instruct the missing tools (`bootstrap`, `shutdown`, `orchestration-common`, `orchestration-track`, both memory templates, `AGENTS.md`). Scope: adopt the seven into `scripts/`; deploy through the installer under the existing sha256-manifest discipline; supersede the global copies (`test_realhome_supersede.py`'s retired-referencer gate extended to the seven names, reusing the CR-MDB-016 §C2 pattern). Ceiling on supersede: the leftovers are chezmoi-managed and a plain `rm` resurrects them on apply, so the installer REPORTS the exact stale paths and the `chezmoi destroy` remedy — Model B deletes nothing under `~/.claude` and runs no chezmoi op.
- 2026-08-27 — TOON OWNERSHIP resolved inside CR-MDB-022 §S2/§S3, because adoption forced it. THREE implementations exist: Crucible's `clients/toon.py` (1356 lines); `~/.claude/scripts/toon.py` (254 lines), which `worktree-flow.py:117` self-describes as "copy of `crucible:clients/toon.py`"; and `modelb_axi/axi.py` (95 lines), Model B's own encode-only 4-construct subset whose docstring at `:10-12` states it deliberately never imports Crucible's. Adopting a self-declared copy of a Crucible file would contradict the same-day no-client-maintenance directive, so Model B keeps ONE codec in `modelb_axi/` (encoder extended with a decoder) and `scripts/toon.py` becomes a thin re-export. This also explains 3 of the 7 baseline test failures: `tests/test_worktree_flow_axi.py:39-45` inserts `~/Documents/data_projects/crucible/clients` on `sys.path` and uses CRUCIBLE's 1356-line decoder as its oracle against output from the 254-line encoder — `worktree-flow.py` emits `rows[0]:`/`help[0]:` (empty-list headers) and the oracle refuses them with `ToonDecodeError: Line 7: Expected 1 list-form items, but got 0`. A real encoder defect, latent because the tool lived outside the repo where no gate ran. **Third finding, same seam:** the governing subset note `DN-crucible-toon-subset.md` is **CRUCIBLE's** (`crucible:docs/research/`), not ours, and `modelb_axi/axi.py:12` cites it by bare filename with no anchor — the unanchored-cross-project-reference defect CR-MDB-020 fixes for client paths, here in code. Model B may not edit it, so the subset Model B honours (including the empty-array form) is recorded on the Model B-owned `contracts/crucible-envelope.md`, the citation is anchored `crucible:`, and any divergence from their note is raised on the #1336 lineage rather than forked silently.
- 2026-08-27 — SURFACED, not filed (needs a decision): the `code-health` skill is Model-B-workflow by content — it speaks of MAINLINE, cull-CR ratification and defined snapshot points — and it consumes the three `rust-*` tools CR-MDB-022 adopts, yet it is NOT one of Model B's 13 published bundles and exists only in the user's skill tree. CR-MDB-022 deliberately adopts the tools without it (recorded in its Non-goals); whether that skill should become a published Model B bundle is an open call. Also outside the seven named and unmanaged by chezmoi: `gate-lock.sh` — which is the gate-coordination tool `orchestration-common.md` §Workflow-gates describes — and `gh-run-watch.sh`. Electronics tooling in the same directory (`elk_*`, `sch-*`, `skidl_*`, `layout-clip-probe.*`) stays EXCLUDED per standing rule.
  - **RESOLVED 2026-08-27 (same day, user directive — see below):** both are ADOPTED. `gate-lock.sh` → CR-MDB-022 (eighth script); `code-health` → CR-MDB-023 (published bundle). The "13 published bundles" figure in this note is also wrong — `skills-src/` holds 14. `gh-run-watch.sh` and the electronics tooling remain out of scope.
- 2026-08-27 — **USER DIRECTIVE: detach from the local development machine's environment.** The pattern: Model B owns its scripts under `scripts/`, bundles them with the skills it manages, publishes them, and every reference resolves to Model B's own folder — chezmoi's ownership on the local machine is not Model B's problem. **CR-MDB-022 is therefore the FIRST CR of Wave 5** (P0, dependencies cleared: its former edges on 018 and 020 were conservative serialization, and 020 gained `Depends on: 022` instead). One correction to how "overwriting the local machine environment" is implemented: the installer must NOT write into `~/.claude/scripts/`, because that path is chezmoi-managed and anything written there is reverted on the next `chezmoi apply` — a deployment that silently disappears reads as a tool defect and is unfixable from this repo. Deploy to Model B's own store (the CR-MDB-016 §C2 shape) and point every reference there; that is what detachment achieves, and it keeps Model B out of the user's dotfile tree as two standing rules require.
- 2026-08-27 — **USER DIRECTIVE: adopt `code-health` and `gate-lock`.** Split by deliverable type, both at the head of the wave. **`gate-lock.sh` folds into CR-MDB-022** (eighth script, same asset class, same detachment) and brought two findings: (i) `skills-src/model-b/references/orchestration-common.md:63` specifies the entire serialize-heavy-gates protocol and **never names the tool that implements it**, so a track following the published rule cannot find the binary — the CR names it plus its seven verbs and four exit codes; (ii) the lock path `<git-common-dir>/nai-gate.lock` (`/tmp` fallback) is a **cross-project wire contract**, not a stray project name to clean up — Crucible's `rust-crucible.py` is the gate-runner half, owns the lock file (`_acquire_gate_lock`, 2h abandonment at `:111-113`) and names our script in its help at `:363`. A one-sided rename makes both tools wait on a path the other never writes, both conclude the gate is free, and two heavy regressions run concurrently — the exact env-kill the rule exists to prevent. So 022 RECORDS it in a new `contracts/gate-lock.md`, preserves `nai-` verbatim, and any rename goes to Crucible as a CReq. **`code-health` becomes CR-MDB-023** (a published bundle, dep 022 for the deployed tool path): it calls itself "Tools (Model-B toolset, `~/.claude/scripts/`)" yet is unpublished and hard-codes local-machine paths at `:26`, `:29-32`, `:35`, `:38`, `:41`. It also closes drift the 2026-07-20 audit already measured and nobody could fix while the skill sat outside the repo — `rust-dead-scan.py`'s `hot-path` mode is missing from a list that reads as complete (`audit:15`, `:31`), and the ledger is documented cull-only when the tool is multi-domain (`--domain {cull|temporal|<name>}`, `audit:14`). Bundle accounting is corrected in passing: `skills-src/` holds **14** bundles, not the 13 `AGENTS.md` claims, and `tests/test_installer_assets.py:94`'s `ALL_SEVEN_BUNDLE_NAMES` becomes a misnomer at 8 owned bundles.
- 2026-08-27 — **USER DIRECTIVE: `schedule_db.py` is transitional and will be deprecated.** It was the interim workflow planning/scheduling DB; Crucible assumes responsibility for storing the workflow plan and state from their **0.2.0** (unreleased — never to be cited as available, per #1359). CR-MDB-022 therefore adopts it so the tooling bundle is complete and usable today, banners it with the successor and the trigger release, and **invests nothing further in it**: no DB fixture, no new verbs, no schema work, no widened tests. Its tested path stays the `schedule_db unavailable — queue-only project` degrade, which is also what a post-0.2.0 world makes permanent, so `worktree-flow.py` needs no rework when the storage moves. The retirement of the DB half of `worktree-flow.py` is a future CR gated on that release actually shipping, and CR-MDB-023 is barred from deepening the coupling while documenting it.
- 2026-08-27 — **CR-MDB-022 SHIPPED (merge `3c3dbdc`)** — the wave-5 detachment precondition is done. C1: the eight workflow scripts adopted into `scripts/` (adoption fidelity verified by diff against source; mode bits exact) and shipped through a new installer asset class `.agents/scripts` (`deploy.py::TOOL_SCRIPTS_STORE_RELDIR`, `_tool_scripts()`, `[install].tool_scripts_dir`), deployed once user-scope with NO per-harness symlink. C2 (RED restarted once, see below): `modelb_axi/toon.py` is the ONE hand-maintained TOON codec, `axi.py` delegates to it and no longer defines `encode`, and `scripts/toon.py` is a GENERATED artifact drift-gated by `generator/build.py --check` — a thin re-export was impossible because `modelb_axi` lives in an isolated uv-tool venv while the deployed tool runs under bare `python3`. C3: `orchestration-common.md` finally NAMES `gate-lock.sh` (it specified the whole gate protocol without naming the tool), `contracts/gate-lock.md` records the cross-project lock contract, and the six real instructional `~/.claude/scripts/` references are repointed to `~/.agents/scripts/`. V1 VERIFY APPROVE with no blocking items — independently re-derived, mutation-probing the drift gate, fuzzing 400 cases of the codec against Crucible's port out-of-process, checking the lock path from both a main tree and a linked worktree, and BUILDING A WHEEL to confirm all eight assets ship (the CR-MDB-014 defect class). Pre-merge gate 227 passed / 1 failed / 12 skipped of 240. **Suite baseline moves 194/7F/11S → 240/1F/12S**: the codec fix cleared 6 of the 7 prior failures — the 3 `test_worktree_flow_axi` envelope gates AND, unpredicted, the 3 `test_scaffold` envelope gates that decode `axi.py`'s output. The 1 remaining failure is the pre-existing `test_crucible_skill` AC7 `WORKFLOW_CYCLE_ID` claim in `AGENTS.md`, proven pre-existing by diffing that line against `develop`.
- 2026-08-27 — CR-MDB-022 process record, kept because the corrections cost real time. FOUR of the spec's own claims were falsified by measurement and fixed in-flight: (i) the §S3 defect diagnosis blamed empty list headers `rows[0]:`/`help[0]:` — they are VALID and `status` always decoded; the real defect was a NON-EMPTY `warnings[1]:` header followed by BARE indented items, which `crucible:clients/toon.py:1243` refuses because it counts an item only when the line starts with `- `; (ii) `DN-crucible-toon-subset.md` is RETIRED (their CR-CRU-046, 2026-08-01) and the wire contract is the OFFICIAL TOON spec, so there was no private 4-construct subset to mirror and `axi.py`'s docstring claim was stale — the gap-analysis had confirmed that DN's location and owner but never read its contents, which Dimension 1 requires; (iii) §S2's "thin re-export so a directly-invoked script keeps working" was self-contradictory, caught by measuring that `modelb_axi` is not importable from a bare `python3`; (iv) §S6's scan scope listed `docs/changes/`, making it unsatisfiable — a spec cannot forbid a string and also document the prohibition. Also settled: **there is NO usable generic Python TOON library** — PyPI `toon-format` 0.1.0 is a name-reservation stub whose `encode`/`decode` raise `NotImplementedError`, and PyPI `toon` 0.15.9 is an unrelated neuroscience package; Crucible carries a landmine guard against re-pinning either. Model B stays stdlib-only with zero runtime dependencies. Four inherited type-preservation defects were fixed as part of C2, one of them LIVE: our envelopes carry `context.wave` as the string `"4"` and the old subset decoder promoted it to an int.
- 2026-08-27 — DEFERRED register, filed at CR-MDB-022 close-out (next SCRUM decides): (i) `tests/test_worktree_flow_axi.py` still drives the STALE `~/.claude/scripts/` copies, which emit the defective bare-item form the new decoder deliberately tolerates — so its six greens never exercise C2's fixed encoder and §S3/AC8 is met in letter only; (ii) the same file at `:99` carries a literal `~/.claude/scripts` `sys.path` insert inside a SUBPROCESS STRING, which the §S6 AST gate structurally cannot see, and `tests/` is an excluded surface, so it slipped both gates; (iii) §S4/AC4's "a dry-run writes nothing" is VACUOUS for the scripts asset class because the installer flow has no `--dry-run` (that flag belongs to the scaffold/init flow) — spec hygiene, not a gap. Items (i) and (ii) are one CR's worth of work and are the same CR-MDB-010-era shape; they became misleading rather than merely stale the moment C2 fixed the encoder. Also standing: the repo has no `app/` source, so `pre-merge-gate` coverage collects no data — CR-MDB-012 must settle the coverage stance rather than inheriting a silent warning.
- 2026-09-09 — **CR-MDB-017 §S6 CONTENT SOURCED DIRECTLY FROM CRUCIBLE** (Sandesh #1362–#1366). Two-round correspondence, one correction accepted on our side: Mainline first attributed 16 patched-in-place agent-definition files to Crucible's session; independently re-verified (separate inodes, mtimes, `--phase` counts) and it was WRONG — pre-existing generator drift in this repo, and Crucible's own edits live in a separate tree they own (`~/.omp/agent/agents/`), untouched `~/.claude`. Reconciled split: **16 files are Model B's** (arduino/bun/python/quarkus × 4 roles); the four `~/.claude/agents/rust-*-agent.md` files belong to NEITHER generator (`generator/stacks/rust.toml` does not exist here; Crucible's own rust set is separately located and already fixed) — orphaned, flagged to the user, explicitly out of CR-MDB-017's scope. §S6 is now specced with real content rather than a diagnosis: the registration line (`--role`/`--cycle`, agent id already correctly sourced from the dispatch prompt in the existing templates) plus a SECOND independent defect — tier guidance was a single generic block, factually wrong for 3 of 4 stacks, and is now split shared-template / per-stack-TOML per Crucible's own fleet mapping (arduino's three build systems + HIL unreachability, bun/python's declared-target requirement, quarkus's surefire/failsafe split). Content is sourced, not re-derived from `--help` alone, because Crucible hit the assigned-vs-minted agent-id defect themselves (5 mis-attributed runs) before flagging it to us.
- 2026-09-09 — **CR-MDB-024 FILED** (Sandesh #1367): Crucible's user directed them to ask Model B to take ownership of the four orphaned `~/.claude/agents/rust-*-agent.md` files as a fifth generated stack, citing their own CR-CRU-042 handover ("Model B now owns the skills component in FULL — content, bundling AND deploy... Crucible does not patch either in place") as the reason this falls inside our domain. Verified the citation against their actual merged CR text before accepting — it holds verbatim. Rust is not foreign territory: CR-MDB-022 already adopted the three `rust-*` tools and `skills-src/memory-templates/rust-orchestration.md` already documents rust mechanics; only the generated agent-definition layer was missing. Depends on CR-MDB-017 (introduces the `tier_guidance` key + shared/per-stack template split this CR's fragment consumes). Crucible sent the fifth TOML fragment (Cargo's own unit/integration split, `smoke-test`/`docker-e2e-gate` tier mapping per their CR-CRU-111 pin) unprompted in the same message.
- 2026-09-09 — **USER DIRECTIVE: OMP as the preferred harness, tied to the Roundhouse migration.** Model B's harness roster (`claude-code`/`hermes`/`pi`/`opencode`) does not include OMP — this orchestrator's own harness. Measured, not assumed: OMP's agent-definition frontmatter (`~/.omp/agent/agents/arduino-fix-agent.md`) drops `effort`, `tools` and the declared `skills:` array entirely versus this repo's generated Claude Code shape — a real schema divergence. Skills are already largely reusable via the existing `~/.agents/skills/` neutral store; agent DEFINITIONS are not. **Filed as CR-MDB-025, Wave 6 (post-0.1.0), by explicit user scoping decision — does NOT block or reshape the in-flight CR-MDB-017/024 Claude-Code-only work this wave.** Mirrors the `hooks.py::compile_wiring()` neutral-schema + per-harness-emitter pattern already proven in this repo for hooks. Everything about OMP's conventions in the spec is flagged as measured from one developer machine and requiring independent confirmation before GREEN — binary name, skill-directory convention, and agent-discovery path all need verification against OMP's own documentation.
- 2026-09-16 — **CR-MDB-026 FILED — watcher-launch defect, measured not theorised.** The `bootstrap`/`shutdown`/`sandesh.md` instruction to launch the Sandesh wake watcher as a backgrounded shell job was executed exactly as written this session and the watcher was killed at 300s by the harness job deadline, mid-poll, twice, with no mail involved — while its own timeout is 14400s (a 48x mismatch). The bug is a CORRECTNESS one, not tidiness: a deadline-kill is indistinguishable from the mail-arrival exit that IS the wake mechanism, so Mainline silently stops waking on Track mail while a blocked track HOLDS forever (a track is structurally forbidden from self-polling), and every kill opens a gap in which arriving mail produces no wake at all. Relaunching under a supervised named process with readiness gated on the watcher's own `watching <address>` banner fixed it and also collapsed "launched" and "actually listening" into one observable fact, closing the `listening:false` trap the bundles currently handle with a manual re-probe. Three files must move in one commit (the instruction appears in all three); `run_in_background` survives as an explicitly-labelled fallback for harnesses with no supervisor, with its consequence stated. Added to CR-MDB-012's dependency list.
- 2026-09-16 — **Crucible 0.2.0 RELEASED** (Sandesh #1369) — lands on three already-filed pending CRs rather than needing new ones. (a) **`gate-run` is the streaming standard**; `gate-report` is one-shot and now emits a `prefer-gate-run` discouragement warning — `skills-src/crucible/SKILL.md:97` names `gate-report` and is CR-MDB-017's to correct (this also explains why our gate card historically rendered in one shot at the end rather than live-updating). (b) **`clients/crucible.toml` + `clients/STATUS-CONTRACT.md` are the authoritative declared config/contract, shipped as package data** — that is the real re-pin target CR-MDB-019 was filed to find, and the discovery path CR-MDB-018 persists. (c) **Release closure is a client verb** — `milestone --crs --packages --released-at` (CR-CRU-129, "a milestone is a record, not an event"), which is how 0.2.0 itself was closed and what CR-MDB-012's release procedure must use. Also: every flag self-describes (CR-CRU-128) so skills should point at `--help` rather than paraphrase — reinforcing CR-MDB-020's anchoring thesis; TOON was removed from the SERVER (CR-CRU-132) but clients still ship `toon.py` and the AXI envelope contract is unchanged, so CR-MDB-022's codec work stands with no action. **`vscode-crucible.py` did NOT ship, was never filed, and must NOT be built** — declined in #1370 per user directive: **a Crucible client targets a LANGUAGE STACK, and VS Code is an EDITOR, not a stack.** The language is TypeScript, already covered by `bun.toml`; the four deployed `~/.claude/agents/vscode-*-agent.md` files are an editor/toolchain OVERLAY on the TS stack (npm + vitest + `@vscode/test-electron` instead of `bun test`), which is exactly why they have no client and correctly use the direct v2-API path documented in the Crucible-authored `crucible-report-vscode` bundle. **A first draft of this note and a CR-MDB-027 "vscode as a sixth stack" both made the category error and were corrected/deleted before commit** — the orphan-generator question for these four files is real but is NOT the rust case (CR-MDB-024) and must not be framed as a stack adoption; the `crucible-report-vscode` skill is reviewable independently of any stack or client question. Crucible has 0.3.0/wave 7 open; nothing is queued there for us.
- 2026-09-16 — **0.2.0 items 1–2 RECOVERED (#1372) and CR-MDB-017 §S6 AMENDED accordingly.** Mainline destroyed the head of #1369 by piping the fetch through `tail`; `--peek` only renders UNREAD mail, so a fetched message cannot be re-read — the loss was unrecoverable locally and Crucible resent the bytes verbatim on request. **Standing rule reinforced: fetch mail to a FILE and read the file; never pipe a fetch through `head`/`tail`.** The two recovered items both bear on CR-MDB-017 and one changed its spec. (1) **The tier vocabulary is now fleet-uniform** (CR-CRU-111 §S1): the six tier verbs were collapsed into ONE shared registrar, so all five clients carry exactly `unit module integration e2e bdd regression` plus `test`/`pre-merge-gate`/`check` — per-client verb counts at `70224d6` are bun 35, python 35, rust 43, mvn 37, arduino 35. This CONFIRMS CR-017 §S6's shared-preamble/per-stack split rather than contradicting it, and sharpens why the split exists: **the VOCABULARY is uniform, the HONOURABILITY is not** (CR-CRU-111 §S2 — a tier a stack cannot honour must say so). §S6b was amended to name the six tiers and the stale "16-line preamble" count dropped (an artifact of the old generic block); two ACs added — the preamble must name the uniform vocabulary with no stack-specific run verb in its place, and every stack's `tier_guidance` must state which tiers it CANNOT honour, with arduino naming HIL as unreachable from the native host. (2) **Our wave-4 ask for universal plan/cycle verbs is DELIVERED** on all five clients: `cr-plan cr-depends cr-supersede cr-void cr-close plans plan-file plan-backfill queue queue-file next wave-sequence release-propose status cycle-add cycle-activate cycle-done checkpoint abort stop register unregister auto-ingest gate-run gate-report milestone`. Also confirmed by EXECUTION on their side (not source reading): existing `regression`/`unit`/`e2e`/`module` invocations in our seven bundles and the arduino/quarkus agent definitions still exit 0 — **no urgent edit anywhere**; a naive grep for `add_parser("regression")` returns nothing purely because of the shared registrar, which misled Crucible's own first pass too. vscode is settled on both sides: they filed nothing, held no slot, and accepted the wrong-axis reasoning over the cost argument.
- 2026-09-16 — **CR-MDB-025 REWRITTEN against OMP's own documentation and PROMOTED to wave 5** (user directive: "I want our definitions etc be targeting for deploy to OMP"). Researched via `omp://` harness docs at **omp/18.2.1** plus the local install — not inferred. **The blocking fact: OMP will NEVER read our generated agent definitions.** `omp://task-agent-discovery.md` — "Direct cross-harness roots such as `.claude/agents`, `.codex/agents`, and `.gemini/agents` are **intentionally skipped** — their frontmatter schema is not the OMP task-agent contract (`TASK_AGENT_CONFIG_SOURCE = \".omp\"`)". A deliberate refusal with no toggle; OMP reads only `~/.omp/agent/agents/*.md` (user) and `.omp/agents/*.md` (project). **Second blocking fact found while verifying the first: `deploy.py` has NO agent-definition asset class at all** — it deploys skills, hook scripts and tool scripts; `generator/agents/` is never installed, so this CR ADDS a class rather than retargeting one. **SKILLS ARE ALREADY OMP-CORRECT — no work, proven empirically:** `omp://skills.md` registers the `agents` provider (`.agent[s]/skills`, priority 70) as "the canonical OMP-native location", `~/.agents/skills/` is our real store (21 bundles) with `~/.claude/skills/<name>` symlinked into it, and THIS orchestrator session is an OMP session whose discovered skills include `model-b`/`crucible`/`bootstrap`/`shutdown`/`cr-authoring`/`git-workflow`/`chezmoi`/`crucible-report-*`. CR-MDB-002/014's harness-neutral store made skills OMP-correct as a side effect of being neutral; skills are an explicit NON-GOAL so nobody "fixes" them. **The earlier draft's frontmatter "measurement" was WRONG IN BOTH DIRECTIONS and is recorded in the spec as §S0 so it cannot recur:** it claimed OMP drops `effort`/`tools`/`skills` and has `color`/`maxTurns`. Truth from `parseAgentFields()` — OMP HAS `tools`; `effort`→`thinking-level`; `skills`→`autoloadSkills` (renames, not removals); `color` and `maxTurns` are in NO contract (preserved as inert unknown metadata). The error came from sampling ONE file in `~/.omp/agent/agents/`, a 30-file tree of MIXED provenance (16 carry `maxTurns`, 16 `color`, 10 `skills`, 10 `effort`, 9 `thinking-level`, 24 an `authority` key in no contract). **Method rule: the contract comes from `omp://` docs + documented parser behavior, never from sampling deployed files.** Silent ineffectiveness is the failure mode to fear, not a crash — a wrong field name yields a definition that looks configured and does nothing. Also real work: `tools` needs NAME TRANSLATION (Claude `Read/Grep/Glob/Bash` → OMP `read/grep/glob/bash` + `ast_edit`/`lsp`/`hub`/`eval`; `yield` is auto-added and must not be emitted), `model: sonnet|inherit` → `@role` aliases resolved through `modelRoles` in `~/.omp/agent/config.yml` (which also satisfies **Roundhouse PRD §D4**'s Tier-1 route map), and an OMP hook emitter writing `~/.omp/agent/hooks/pre|post/*.{ts,js}` factories (`omp://hooks.md`: a factory directly in `hooks/` loads NOTHING silently) where **`fail_direction: closed` IS honourable** — `tool_call` blocks on `{block:true}` and the wrapper fails closed on a handler throw — so security-class hooks must NOT be refused for OMP. `~/.omp/agent/hooks` does not exist today, so no Model B hooks reach OMP at all. Depends on CR-MDB-017 (same templates/frontmatter surface); Claude Code output must stay BYTE-IDENTICAL; the integration gate requires OMP to RESOLVE an emitted definition by name at runtime, not merely parse it. Added to CR-MDB-012's dependency list; the wave-6 section is gone (it held only this CR).
- 2026-09-18 — **PRODUCTION CRUCIBLE ADOPTED; target release renamed 0.1.0 → 1.0.0; the declared-roadmap
  verbs (`release-propose`/`cr-plan`/`wave-sequence`) used for the first time.** Crucible migrated Model
  B's whole slice (90 events, 16 plans, 47 cycles, 19 queue entries, 10 milestones) from their
  development board to the local production install (Sandesh #1373), verified row/column-exact, and
  corrected the project's stale `sut_root` to this repo's Roundhouse path on request (#1374/#1375).
  User directive: propose a `1.0.0` release and target every filed CR (001–026, both shipped and
  pending) at it. `release-propose --label 1.0.0 --target 2026-10-31` filed; all 26 CRs planned via
  `cr-plan --release 1.0.0`. **The ROADMAP wave structure is deliberately NOT this file's wave
  column** (user ruling, after the first attempt mirrored waves 1–5 onto the board and produced
  incoherent rows — old waves holding PENDING entries). Our 1–5 waves are a HISTORICAL delivery
  structure that ~15 footer notes below reference ("WAVE 2 CLOSED", "WAVE 5 OPENED"); a release
  roadmap needs a forward-looking one. The board therefore carries exactly TWO waves:
  **wave 1 = the 16 CRs already done** (001–016, 022), **wave 2 = the 10 still to do** (017, 018,
  019, 023, 020, 024, 025, 021, 026, 012 — in dependency order, 012 last). `next` now answers
  `NEXT cr=CR-MDB-017 seq=2001 wave=2 waveCompleted=1`. This file's own wave column is UNCHANGED
  and stays historical; the two structures are intentionally different and must not be "reconciled".
  **Pre-v2 metadata repaired (user-authorised to default reasonably rather than wait on Crucible).**
  Two state errors were on the board, both pre-existing in the migrated slice, neither created here:
  (i) `CR-MDB-001` derived `PENDING` despite shipping 2026-07-20, because its two plans (14, 15) are
  keyed to the PRE-RENAME ids `CR-MB-001` and `CR-MODELB-001` — corroborated by the merge commit's
  own branch, `feature/CR-MODELB-001-core-split` (`4fd2fcadc67f…`), and by the 2026-07-20 footer note.
  No client verb re-keys a plan, so the repair was to file a correctly-keyed plan (91) carrying the
  history this file already records (C1 red-green, C2 verify), walk both cycles through their legal
  transitions, and `cr-close --commit 4fd2fca…`; the row now reads `COMPLETED, planId 91`. A plan
  with invented cycles was NOT manufactured — every declared cycle matches the recorded history.
  (ii) All 16 plans carried waves `null`/2/3/4/5, inconsistent with the new roadmap; `plan-backfill
  --wave 1` aligned every one (it overwrites an existing wave, verified on plan 18 before bulk use).
  `CR-MDB-007` is recorded `cr-supersede --by CR-MDB-006` (absorbed by 002–006, no spec ever filed),
  which also auto-resolved CR-MDB-012's dependency on it; its row still derives `PENDING` beside
  `lifecycle.state: SUPERSEDED` because derived status answers from plans and knows nothing of a
  disposition — read lifecycle for the disposition, not status.
  **Own error, disclosed:** getting 001's merge onto the board before the plan repair, `--label` was
  used for an explanatory sentence, and `crMerged` renders labels — so that list now shows 001 twice,
  once cleanly from `cr-close` and once as a sentence. No second milestone was posted to "fix" it (no
  documented convergence semantics; a duplicate is worse than an ugly label). Retraction of the
  manual one, and disposal of the two now-redundant orphan plans, are asked of Crucible over Sandesh.
  **STANDING RULE ESTABLISHED (user directive 2026-09-18, corrected mid-session):** Model B has
  NOTHING TO DO with the local Crucible PROJECT at any layer — not its development server (port
  3850), not the client-side scripts living inside that project's own checkout
  (`~/Documents/data_projects/crucible/clients/`), and not that checkout's own docs for reference
  reading. The ONLY sanctioned surfaces are the PRODUCTION server (port 3849) and the PUBLISHED,
  INSTALLED clients at `~/.crucible/clients/<stack>-crucible.py`. Measured cause of the near-miss:
  `skills-src/crucible/SKILL.md:51` documents the dev-checkout path as canonical, and that checkout
  carries an operator `crucible.toml` (`[client] url = "http://127.0.0.1:3850"`) which resolves as
  the "install" tier of `_crucible_axi.py`'s project→install→shipped chain and wins before the
  shipped default — so following our OWN skill exactly as written binds to Crucible's dev board, not
  production. Caught before any write landed on the wrong board; this session also read one Crucible
  dev-checkout doc for background (`docs/changes/CR-CRU-118-*.md`) BEFORE the rule was stated
  explicitly — noted here as now out of bounds, not to recur. **CR-MDB-020's client-path-anchoring
  work must repoint `skills-src/crucible/SKILL.md:51` and every bundle citing the dev-checkout path
  at the installed production location** — a concrete, measured finding for that CR's scope, not a
  hypothetical.
  `docs/changes/README.md`'s own header/queue-row/ships-here banner renamed to `1.0.0` in this same
  edit; historical footer notes describing the PAST target as `0.1.0` are left as-authored (a dated
  journal, accurate to its own time) rather than rewritten. `CR-MDB-012`'s spec file — never
  committed (found this session) — will be authored at the next SCRUM from Crucible's preserved title
  and 11-entry 2026-05-25 dependency snapshot (now 18 in this file, confirmed a strict superseding
  prefix, not a disagreement) plus the current 1.0.0 label.
